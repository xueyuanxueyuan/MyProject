# 2026-04-08 至 2026-04-09 工作日志总结

## 一、 今日工作进展（2026-04-09）

### 1. 财务核算 (cwhs) - 月结功能增强
- **月结定时任务**：实现了月结新增定时任务逻辑 (`YjAddJobHandler`)。支持多账套的动态获取，并且增加了超前月结拦截机制，进一步提升了系统自动月结的健壮性。

### 2. 业务管理 (ywgl) - 贷款资金调拨优化
- **银行冲突双重校验**：为了防止资金调拨出现重复问题，新增了同日“同付款银行+收款银行”组合校验。不仅拦截申请内的重复，还校验当日历史重复记录；同时在提交审批和重新提交审批前，增加了“拨付途中”银行冲突校验，坚决阻止冲突申请进入审批流。
- **状态及时流转**：补充完善了贷款资金调拨在提交审批后的状态更新逻辑，确保流程节点状态实时准确。

### 3. 业务管理 (ywgl) - 黑名单账号管控升级
- **黑名单有效期管理**：为枣庄黑名单账号功能新增了“开始日期”和“结束日期”字段。同步更新了相关 DTO、实体类 (Entity)、持久层 (Mapper/XML) 以及对应的初始化 SQL 脚本，实现了黑名单的时效性精细化管控。

### 4. 基础框架与环境配置
- **预提设置与配置更新**：修正了预提设置查询包装器的使用方式，同时对多个环境的配置文件（如 `application-city-tsp.yml`、`application-city-zaozhuang.yml`、`application-zaozhuang-dm.yml`）进行了大幅同步与完善。

---

## 二、 昨日工作回顾（2026-04-08）

### 1. 资金结算/业务管理 (zjjs/ywgl) - 一户通(YHT) CAPS协议对接【核心大版本】
- **完整业务模块落地**：从零到一构建了“一户通”与 CAPS 平台对接的完整架构，新增近万行代码，包含以下核心模块：
  - **核心服务层**：构建了主业务服务、重试机制、补查服务、幂等服务、状态机流转、人工干预介入服务、以及告警和费用映射等全套生态。
  - **协议与安全层**：完成了 CAPS 消息头构建、客户端切面拦截、签名/加密、证书管理、敏感数据脱敏等安全处理。
  - **调度与监控**：集成了重试、超时检查、协议查询、心跳探测等一系列定时任务，同时落地了全面的业务日志与异常体系。

### 2. 业务管理 (ywgl) - 贷款资金调拨全链路完善
- **重新提交审批**：打通了审批驳回后重新提交（`resubmitDkZjdbSp`）的业务闭环，包含严格的主键、任务及流程实例 ID 校验。
- **审批意见与历史查询**：
  - 新增了审批历史查询功能，支持获取最新驳回意见。
  - 添加了根据结算流水号查询收款业务审批意见列表的功能。
- **放款银行信息查询**：打通了根据账户 ID 查询放款银行信息、账户余额、备付金信息的全流程接口与联调。

### 3. 财务核算与外部对接维护
- **财务核算 (cwhs)**：调整了银行存款日记账的“对方科目”展示逻辑，从科目名称改为科目编号展示，并统一使用中文顿号作为多值分隔符，成功规避了因不同科目名称相同导致的错误合并问题。
- **住建部接口 (wbjk)**：微调了住建部服务与账户变动台账之间的映射关系及底层 Socket 字节解析服务。

---
**总结与反思**：
这两天的工作含金量极高！昨天攻坚拿下了“一户通”CAPS协议对接这一超大核心模块，同时极大丰富了贷款资金调拨的业务闭环（如审批历史、重提等）；今天则在此基础上，进一步加固了资金调拨的防并发防冲突校验，并完成了黑名单时效性管理和月结定时任务的底层构建。系统正变得越来越安全、智能和健壮。
