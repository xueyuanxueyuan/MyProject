# 枣庄全量服务打包发布记录（2026-04-03）

## 目标
枣庄环境后端全部 `*-app.jar`、前端 `capinfo-gjj-frontend-jshs-gm.zip` 编译打包后全量上传，并批量滚动重启 `zzgjj-test` 命名空间内相关 deployment。

## 执行摘要
1. **后端**：`zaozhuang/IdeaProjects/capinfo-gjj-busi-jshs`，`JAVA_HOME`/`PATH` 使用 `~/.vfox/cache` 下 JDK 17 与 Maven 3.9.14，`mvn clean package -DskipTests -s settings-tsp.xml`，结果 `BUILD SUCCESS`。
2. **前端**：`zaozhuang/WebstormProjects/capinfo-gjj-frontend-jshs-gm`，Node 16.20.2，`npm run build` 后 `zip -r capinfo-gjj-frontend-jshs-gm.zip dist`。
3. **聚合**：`mkdir -p zaozhuang/release`，`find ... -name "*-app.jar" -exec cp`，复制前端 zip 至 `release`（共 8 个文件：7 个 jar + 1 个 zip）。
4. **上传**：`python3 scripts/remote_deploy.py zaozhuang`（全量上传至枣庄测试服务器）。
5. **重启与校验**：按 `doc/skills/gjj-remote-deploy` 示例对 8 个 deployment 执行 `kubectl rollout restart`，并依次 `kubectl rollout status ... --timeout=300s`，全部 `successfully rolled out`。

## 制品清单（release）
- `capinfo-gjj-busi-cwhs-jzgl-basic-svc-app.jar`
- `capinfo-gjj-busi-jhgl-zjjh-basic-svc-app.jar`
- `capinfo-gjj-busi-jshs-gm-agg-svc-app.jar`
- `capinfo-gjj-busi-zjjs-lcgl-basic-svc-app.jar`
- `capinfo-gjj-busi-zjjs-sp-app.jar`
- `capinfo-gjj-busi-zjjs-ywgl-basic-svc-app.jar`
- `capinfo-gjj-busi-zjjs-zhgl-basic-svc-app.jar`
- `capinfo-gjj-frontend-jshs-gm.zip`

## 备注
- 前端构建含既有 warning（如 `getOrgList`、资源体积等），未阻塞发布。
- 本次前端仓库 `git pull` 拉取了 `zaozhuang/dev` 分支新提交（涉及 `cwzjdb.vue`、`dkzjdb.vue` 等）。
