# 枣庄测试环境上传发布记录

## 1. 任务背景
在完成 `zaozhuang` 地区项目的打包编译后，需要将位于 `/home/source/Jetbrains/Probject/Gjj/zaozhuang/release/` 下的构建制品（包含前端压缩包及多个后端的 `*-app.jar` 文件）上传到“枣庄测试环境服务器”。

## 2. 准备工作与解决思路
1. **获取最新配置**：检测到用户已手动更新 `config/deploy_servers.json` 中的服务器信息：
   - 目标主机：`100.70.0.189`
   - 目标端口：`22022`
   - 用户名密码已配置完毕。
   - 目标发布路径：`/data/app/zzgjj-test/capinfo-gjj-jshs`
2. **处理系统依赖环境**：因系统使用了 `externally-managed-environment` 机制，原有 `pip install paramiko` 被拦截，采用系统的包管理工具 `sudo apt install -y python3-paramiko` 安装解决了依赖问题。
3. **调用远程发布技能**：使用刚建立好的 AI 技能 `gjj-remote-deploy`，自动执行脚本，批量将本地 `release` 目录内的所有文件通过 SFTP 安全上传到服务器。

## 3. 上传结果
所有打包制品已成功发布至服务器，共计上传 8 个文件，传输列表如下：
- `capinfo-gjj-frontend-jshs-gm.zip`
- `capinfo-gjj-busi-zjjs-sp-app.jar`
- `capinfo-gjj-busi-cwhs-jzgl-basic-svc-app.jar`
- `capinfo-gjj-busi-zjjs-zhgl-basic-svc-app.jar`
- `capinfo-gjj-busi-jhgl-zjjh-basic-svc-app.jar`
- `capinfo-gjj-busi-zjjs-ywgl-basic-svc-app.jar`
- `capinfo-gjj-busi-jshs-gm-agg-svc-app.jar`
- `capinfo-gjj-busi-zjjs-lcgl-basic-svc-app.jar`

上传操作均通过验证，成功连接到测试环境。
