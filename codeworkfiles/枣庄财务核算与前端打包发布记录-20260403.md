# 枣庄财务核算与前端打包发布记录（2026-04-03）

## 需求目标
打包并发布枣庄环境的：
- 财务核算服务：`capinfo-gjj-busi-cwhs-jzgl-basic-svc-app.jar`
- 前端服务：`capinfo-gjj-frontend-jshs-gm.zip`

## 执行步骤与命令
1. 工具链检查与切换（vfox）
   - 直接命令尝试：`vfox use java@17.0.2+8`、`vfox use maven@3.9.14`（终端未直接识别 `vfox` 命令）
   - 修正动作：使用绝对路径与 vfox 缓存 SDK 路径完成校验与构建
2. 后端打包（枣庄）
   - 工作目录：`zaozhuang/IdeaProjects/capinfo-gjj-busi-jshs`
   - 命令：
     - `export JAVA_HOME=/home/source/.vfox/cache/java/v-17.0.2+8/java-17.0.2+8`
     - `export PATH=${JAVA_HOME}/bin:/home/source/.vfox/cache/maven/v-3.9.14/maven-3.9.14/bin:$PATH`
     - `java -version`
     - `mvn -v`
     - `git pull`
     - `mvn clean package -DskipTests -s settings-tsp.xml`
3. 前端打包（枣庄）
   - 工作目录：`zaozhuang/WebstormProjects/capinfo-gjj-frontend-jshs-gm`
   - 命令：
     - `export PATH=/home/source/.vfox/cache/nodejs/v-16.20.2/nodejs-16.20.2/bin:$PATH`
     - `node -v`
     - `npm -v`
     - `git pull`
     - `rm -rf dist`
     - `npm install`
     - `npm run build`
     - `zip -r capinfo-gjj-frontend-jshs-gm.zip dist`
4. 发布包聚合到 `zaozhuang/release`
   - `cp` 财务核算 jar 和前端 zip 至 release 目录
5. 远程上传与重启
   - 上传命令：
     - `python3 scripts/remote_deploy.py zaozhuang capinfo-gjj-busi-cwhs-jzgl-basic-svc-app.jar capinfo-gjj-frontend-jshs-gm.zip`
   - 重启命令：
     - `python3 scripts/remote_exec.py zaozhuang "kubectl rollout restart deployment capinfo-gjj-busi-cwhs-jzgl-basic-svc-depl capinfo-gjj-frontend-jshs-gm-depl -n zzgjj-test"`
   - 状态校验：
     - `python3 scripts/remote_exec.py zaozhuang "kubectl rollout status deployment/capinfo-gjj-busi-cwhs-jzgl-basic-svc-depl -n zzgjj-test --timeout=180s && kubectl rollout status deployment/capinfo-gjj-frontend-jshs-gm-depl -n zzgjj-test --timeout=180s"`

## 结果
- 后端构建结果：`BUILD SUCCESS`
- 前端构建结果：`Build complete`
- 上传结果：两个文件均上传成功
- 重启结果：两个 deployment 均 `restarted`
- 滚动状态：两个 deployment 均 `successfully rolled out`

## 风险与说明
- 前端构建出现既有 warning（如 `getOrgList` 引用告警、包体积告警），不影响本次打包与发布完成。
- 终端环境中 `vfox` hook 初始化异常，已通过 `~/.vfox/cache` 指定版本路径完成等效版本控制与构建。
