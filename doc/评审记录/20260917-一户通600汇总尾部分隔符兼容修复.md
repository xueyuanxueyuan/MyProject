# 600 汇总文件尾部分隔符兼容修复

日期：2026-09-17；双项目同步复核：2026-09-18

## 范围与决策

按用户确认，同步修改 prod 与嘉兴后端 YhtReconPersistenceService.persistSummary 的列数校验。保留 split("\\|", -1) 及原字段读取位置，正常 7 列照常接收；仅额外接受 8 列且第 8 列为严格空字符串。第 7 列为空的实时业务仍保留空批次号。第 8 列非空（包括空格）、不足 7 列、超过 8 列继续拒绝。

不修改验签、解密、原始报文、日期、明细解析、数据库结构。该改动解决已知格式冲突，不等于确认现场 E401 的全部原因。

## 回归覆盖

新增 YhtReconPersistenceServiceTest，覆盖 4 个合法输入和 5 个非法输入，验证日期、渠道、交易类型、金额、笔数、批次号、原文保留，以及非法输入不访问汇总 DAO。

嘉兴保留原有 YhtReconPersistenceServiceTest 金额解析测试，另新增 YhtReconSummaryTrailingSeparatorTest，包含与 prod 一致的 9 项边界用例，避免覆盖既有未提交测试。嘉兴代码位于 jiaxing/IdeaProject/capinfo-gjj-busi-jshs，prod 位于 prod/IdeaProject/capinfo-gjj-busi-jshs。

2026-09-18 已核验：两套生产代码均仅改动同一列数条件并删除过时注释；任务范围 git diff --check 通过；两套专项测试除类名外一致；9 项等价边界表达式检查通过。不替代 Java 单元测试，嘉兴新增用例尚未执行，prod 修复后 Java 回归仍未完成。

## 验证证据与限制

- 修复前 JUnit 实际执行 9 项：7 成功、2 失败；两项失败均为多一个尾部分隔符的兼容用例，异常为“汇总文件列数非法, lineNo=1”。
- 修复后 PowerShell 等价边界表达式交叉检查：9/9。此检查不替代 Java 单元测试。
- 修复后 Java 编译未通过：本地依赖访问权限异常；收窄依赖后仍有 spring-context JAR 访问异常，并有 BaseDO 依赖缺失。不能声称修复后 JUnit 通过。
- 尝试 vfox use java@17.0.2+8、vfox use maven@3.9.14；切换受权限限制，3.9.14 未安装；java -version 为 17.0.2，mvn -v 为 3.9.16。未修改全局配置，未执行 Maven 打包。
- 定向验证参数与临时运行器位于业务模块 target/recon-tail-tests；编译命令为 javac @target/recon-tail-tests/compile.args，测试命令为 java @target/recon-tail-tests/run.args。临时产物不提交。
- 根目录交付守卫 scripts/guardrails/agent-delivery-guardrail.sh 不存在，以任务范围差异、编码、边界规则检查替代。
- 仓库原始协议文档已有其他本地修改及空白检查问题，未处理。未发布、未重发银行报文、未提交。

## 后续验收

在依赖及工具链正常的环境对业务模块执行 mvn -Dtest=YhtReconPersistenceServiceTest -DfailIfNoTests=true test。应为 9 项全部通过，再进行部署后的真实回调验证。

嘉兴业务模块执行 mvn -Dtest=YhtReconSummaryTrailingSeparatorTest -DfailIfNoTests=true test，专项用例应为 9 项全部通过。两套均未打包、发布或提交。
