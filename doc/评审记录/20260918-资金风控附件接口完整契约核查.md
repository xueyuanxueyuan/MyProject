# 资金风控附件接口完整契约核查

## 本次故障及此前验证遗漏
用户提供2026-09-18 11:42:32日志，异常链为 R.data -> AttachmentDTO.dataType，无法将 START_OBJECT 解码为 DataType。公共 API 的 DataType 声明 JsonFormat.Shape.OBJECT，序列化为带 code、desc 的对象；标准枚举反序列化不具备对应对象解析逻辑。此前测试只构造 id 与时间字段，未覆盖整个 DTO，是验证遗漏，不能把其通过等同于真实上传通过。

## 请求参数逐项核查
| 项目 | 当前值/方式 | 本地核查结果 |
| --- | --- | --- |
| 上传路由 | POST /attachment/api/v2/upload | 与公共客户端和服务端 Controller 一致，真实 Feign 编码验证 |
| id | null，不发送 | 服务端可选；本次新增附件不覆盖旧附件 |
| bizId | null，不发送 | 服务端可选，保存为空字符串 |
| bizType | ZJFK | 服务端作字符串保存，本地实现未见白名单校验 |
| bizChildrenType | document | 服务端作字符串保存，长度在DTO声明范围内 |
| keyWords | null，不发送 | 服务端可选，保存为空字符串 |
| file | multipart中名为file的附件 | 使用SDK真实编码器验证字段名、boundary与二进制字节 |
| filename | 保留.docx的中文文件名 | 经过真实清理逻辑再编码；后缀和中文均保留 |
| Content-Type | application/vnd.openxmlformats-officedocument.wordprocessingml.document | 请求体附件类型验证通过，不是JSON或Base64上传 |
| 文件大小 | 日志310020字节 | 小于本地20MB限制；线上网关限制未核实 |

bucketName 是适配接口的历史参数，Capinfo采用业务类型与附件ID，不向公共上传接口发送桶参数。未改变认证、用户或中心信息。

## 响应字段审查
完整契约测试通过反射给实际依赖的 AttachmentDTO 全部27个字段赋值，若出现未覆盖类型则测试失败；用实际 R.success 构造包裹结构，经公共SDK真实Feign代理和SpringDecoder读取。字段分组：
- Long：id、createUser、updateUser、size、orgId；数字和字符串形式均验证，9007199254740993保持精确值。
- LocalDateTime：createTime、updateTime；空格分隔、ISO T分隔、小数秒和null已覆盖，非法日期仍失败。
- DataType：对象形式含code、desc，及字符串形式；DIR、IMAGE、VIDEO、AUDIO、DOC、OTHER全部覆盖，null允许；缺失code、未知code、数字和数组拒绝，不静默转换为OTHER。
- String：bizId、bizType、bizChildrenType、keyWords、submittedFileName、group、path、versionId、bucketName、relativePath、url、fileMd5、contextType、filename、ext、icon、createMonth、createWeek、createDay；逐字段验证返回值，另验证可选字段缺省、null及新增未知字段。
- R包裹：实际成功、业务失败、data=null均通过解码验证。线上完整包裹尚未捕获，不能假定其所有字段与当前SDK完全一致。

## 后续接口审查
- 查询使用 GET /attachment/api/v1/query/id/{id}，返回同一附件DTO，享受本次兼容。
- 删除使用 POST /attachment/api/v1/delete?ids=ID，服务端返回 R<Boolean>，验证true响应和既有失败处理。
- 链接使用 GET /attachment/api/v1/previewUrl/{id}，返回 R<String>，验证字符串解码；浏览器可达性与有效期须真实环境确认。
- 下载使用 GET /attachment/api/v1/download?ids=ID，接收原始 Response，仅传单个ID；真实Feign契约验证参数和原始字节，既有测试覆盖流关闭、HTTP错误和空响应。
- SDK还声明了其他未使用的批量接口，本次不扩展调用范围，也不修改其他服务。

## 修复及验证
仅在附件DTO MixIn上增加dataType专用解码，按对象code或字符串匹配合法枚举。继续保留时间兼容、文件名修复及用户已有字段注入改动，不改变其他枚举或全局日期规则。

新增完整契约测试首轮4项中2项复现对象枚举错误。最终共33项针对性测试通过，Failures=0、Errors=0、Skipped=0，API/BUSI/APP编译通过。命令：mvn -Dtest=AttachmentFullContractTest,AttachmentResponseDecodeTest,FileSecurityValidatorTest,MinioStorageSwitchTest,CapinfoFileServiceImplTest -Dmaven.test.skip=false -DskipTests=false -Dsurefire.failIfNoSpecifiedTests=false test -B -o。

## 仍需真实环境核对
测试使用真实Feign代理、契约、multipart编码器、SpringDecoder和项目ObjectMapper，但传输层为内存模拟，没有访问生产文件服务。线上完整响应、SDK部署版本、Bean注入、认证和网关转发、文件服务底层存储、下载链接可达性仍不能声明全部正常。源代码中用户已有javax.annotation.Resource字段注入改动未擅自覆盖；当前日志能执行远端调用，但本地编解码测试不证明完整Spring启动配置正确。

部署验收应使用同一个模板完成上传、保存、重新读取生成报告、下载、查询及删除。不要反复盲传：11:04与11:42的异常都发生在响应解码阶段，附件可能已落盘，应由文件服务日志和附件记录确认后再清理。当前仅本地修复，未提交、推送或部署。
