# 资金风控报告生成缺失数据源排查

## 14:44 日志补充核查

用户后续附件覆盖 13:59、14:11、14:44 三次生成异常。按 `SQL查询失败` 日志计数，三个批次分别为5、37、37次，均指向不存在的有效数据源ID=1；这些是失败调用次数，不能直接当作独立数据项数。

三次最终异常均为 `ztqktj` 对象中缺少 `jxje`，不是此前根对象 `ztqktj` 本身不存在。14:44日志先处理 `ztqktj.gjje`，随后在条件 `ztqktj.jxje>0` 失败。仅凭 LinkedHashMap 类型不能判断对象来自SQL还是默认值/运行时覆盖，日志没有给出对应数据项当前配置或完整结果。

仓库季度指标SQL第301行已有 `ROUND(COALESCE(gj_cur.jxje, 0) / 100000000, 2) AS jxje`；对日志中32个 `ztqktj` 字段引用与仓库总体情况统计SQL别名做静态比对，均能找到对应别名。这不是实际数据库执行验证，也不证明线上配置已经使用该SQL。应读取线上总体情况统计数据项的配置类型、默认值/SQL、运行时输入、结果类型和实际返回字段，不能直接补常量0或猜测字段改名。

线上仍输出旧的 `SQL查询失败` 后继续渲染，与本地取数失败立即终止的修复行为不一致；本地修复未在本任务中发布，日志不能作为该修复部署成功的证据。创建人姓名查询失败另有兜底日志，不能将它当作这次渲染失败根因。

在线配置未修改：电脑工具因无法可靠识别浏览器网址而多次停止。本次仅核查附件及本地源文件。继续处理需要取得当前模板全部SQL数据项绑定，以及总体情况统计完整配置；不得盲目对所有模板全局替换数据源ID。

## 证据与结论

- 2026-09-18 13:36:17 的用户日志有 5 次 SQL 取数失败，均为数据源配置不存在，ID=1。实际查询带 `del_flag='0'`，说明应用未查到有效记录；不能据此区分未迁移、被逻辑删除或应用连接了错误的配置库。
- 此次失败在创建外部数据源连接池前发生，不是已有证据中的连接超时，也不能认定为密码错误或指标 SQL 语法错误。
- `DocumentServiceImpl.getSqlQueryValue` 原先将失败响应和异常转换为 null；`buildDataModel` 不把 null 数据项放入模型，仍继续渲染，最终出现 `EL1008E`，缺少 `ztqktj`。
- 日志中 `ssnd、zwssjd、ssjdyf、ztqktj.gjje` 标签序列与仓库季度模板一致。仓库月度模板使用 `ssyf、xyf、ydztqktj`。这提示月报场景可能误用了季度模板，但日志不包含完整数据库配置，不能断言当前用户选择的业务模板一定错误。
- 实际 DOCX 单元测试证明：现有 SpringEL 配置支持嵌套 Map 属性；模型缺少根键时可复现同类 EL1008E。没有证据要求把所有点表达式改成方括号或关闭 SpringEL。

## 本次代码变更

仅修改报告 SQL 取数错误传播：数据源ID/SQL未配置、失败响应、空响应/空响应数据、执行异常立即终止模型构建，提示数据项编码和数据源ID。原始失败详情写入服务日志，不直接把连接错误文本或 SQL 内容返回前端。

正常单行结果和空列表保持原逻辑，不填零、不补空对象掩盖业务数据缺失。成功查询但返回零行时原有 scalar/first_row 行为未调整。模板变量定义、公式依赖与真实业务口径需要结合实际配置核验，不能由这项局部修复保证完整正确。

## 环境修复顺序

1. 在应用实际使用的资金风控配置库中检查数据源配置及逻辑删除状态。通过项目数据源管理功能恢复正确连接或重新配置；若正确连接的ID不是1，应将当前模板的数据项绑定至该ID，不能硬编码使用默认库，也不能直接把其它环境ID照搬过来。
2. 确认该连接指向指标SQL所需的数仓/业务库，状态为启用，并在应用网络环境完成连接测试和只读指标查询。不要为了凑ID=1而直接写入明文密码。
3. 核对实际模板ID对应的数据项列表，所有 SQL 类型数据项逐项确认数据源、SQL、参数依赖和结果类型。`ztqktj` 为对象时应采用 `first_row` 并返回模板引用的字段别名。
4. 如果生成月报，使用月度模板和月度指标配置（`ydztqktj`，SQL参数 `ssnd、ssyf`）；如果生成季报，使用季度模板和季度指标配置（`ztqktj`，SQL参数 `ssnd、ssjd`），并检查模板所需 `zwssjd、ssjdyf、ssjdzhyf` 的配置及值。
5. `formula_dependencies` 决定SQL可用参数范围，不仅要看请求参数是否存在，还要看依赖声明是否包含SQL占位符所需名称。核验数值类型、空值及公式分母；不凭模板日志认定参数均正确。
6. 数据项单独取数成功后再生成报告，并验证生成记录、文件上传、下载和内容。本次未执行此环境验收。

## 只读诊断SQL

以下不包含凭据字段，不执行写操作。第二条列出所有当前绑定ID=1的数据项，不应据此批量修改所有模板。

```sql
SELECT id, name, datasource_key, db_type, status, del_flag
FROM zjfk_data_source_config
WHERE id = 1;

SELECT template_id, data_item_code, data_item_type, data_item_config_type,
       allow_runtime_input, data_source_id, result_type, key_field,
       formula_dependencies, del_flag
FROM zjfk_report_template_data_item
WHERE data_source_id = 1 AND del_flag = '0';

SELECT item.template_id, item.data_item_code, item.data_source_id
FROM zjfk_report_template_data_item item
LEFT JOIN zjfk_data_source_config source
  ON source.id = item.data_source_id AND source.del_flag = '0'
WHERE item.del_flag = '0'
  AND item.data_item_config_type = '02'
  AND source.id IS NULL;
```

对实际生成模板还需按 `template_id` 查询 `zjfk_report_template_parameter` 和全部 `zjfk_report_template_data_item`，核对上传的 Word 内容；附件日志不足以提供实际模板ID，本文不猜测。

## 验证证据及边界

- 新增 `DocumentSqlDataFailureTest`，先运行6项用例，旧代码4项失败，均因没有抛出应有的异常；2项正常结果用例通过。
- 修复后新增2项真实DOCX渲染用例，报告专项8项通过；合并原附件响应、文件名、存储切换回归后共41项通过，失败0、错误0、跳过0；API/BUSI/APP Maven reactor成功，退出码0。
- 使用已安装 vfox SDK目录中的 Java 17.0.2、Maven 3.9.16，本次未切换/安装SDK。命令为 `mvn -Dtest=DocumentSqlDataFailureTest,AttachmentFullContractTest,AttachmentResponseDecodeTest,FileSecurityValidatorTest,MinioStorageSwitchTest,CapinfoFileServiceImplTest -Dmaven.test.skip=false -DskipTests=false -Dsurefire.failIfNoSpecifiedTests=false test -B -o`。
- 测试模拟外部SQL返回，使用实际 poi-tl 渲染；不是完整应用事务代理或测试环境端到端验证。未验证真实连接、指标SQL、完整Word模板、用户请求参数和报告最终落库。
- `git diff --check` 通过；仓库未提供交付守卫脚本，使用改动范围、占位符、UTF-8/BOM和空白检查替代。没有提交、推送或发布。
