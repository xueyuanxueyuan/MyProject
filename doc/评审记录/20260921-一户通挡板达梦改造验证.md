# 一户通挡板达梦持久化改造验证

日期：2026-09-21

## 交付范围

- 全部运行持久化改为 JDBC 关系表：配置、动账配置、接口记录、协议、交易、批次、场景、动账幂等；另设控制表维护版本、编号、锁及迁移指纹。
- 旧 MockStateSnapshot 仅用于显式迁移，不再作为运行缓存；文件 API 仅保留在只读迁移入口。
- 专用迁移启动配置无网关、回调、SVS Bean；正常启动仅一个 MockStoreService，初始化数据库配置。
- 原 JSON/备份不改动。未执行真实数据库建库、导入、发布或 Git 提交。既有 release/target JAR 未重新打包。

## 验证结果

执行环境：现有 vfox SDK 路径下 JDK 17.0.2、Maven 3.9.16；未重新切换工具链。依赖下载及 Maven 测试通过受控权限执行。

- mvn test：45 项，44 项通过，0 失败、0 错误、1 跳过。跳过项为 DamengAcceptanceTest，未配置真实隔离 DM8 测试库。
- node --test src/test/js/static-page.test.cjs：6 项通过。
- node --check src/main/resources/static/yht-mock/app.js：通过。
- git diff --check：通过；只有既有行尾转换提示。
- 应用 schema-dm.sql 与交付 01-schema.sql 内容一致，已由测试验证。

## 验证内容

- 双实例共享数据、读取对象与数据库分离、逐字段保存及 NULL/布尔/金额往返。
- 长中文 CLOB、历史 ID 与时间保留、跨连接并发动账唯一占位。
- 配置初始化、默认保留、显式覆盖、数据库异常不吞掉。
- 清历史保留规则/配置/动账幂等，记录与状态表清理。
- 迁移空库门禁、同源重复执行、不同源拒绝覆盖、坏 JSON/重复字段/键值不符拦截、后段插入失败整体回滚。
- 既有网关、回调、协议、批量结果、动账发送与去重回归。

## 交付守卫与风险

仓库不存在 scripts/guardrails/agent-delivery-guardrail.sh，采用等价人工扫描：差异、文件读写入口、占位、敏感配置、脚本一致性、真实测试输出和退出码复核。

SQL 表名、字段名和排序来自程序常量，业务值使用参数绑定；只有迁移模式解析 JSON。未把认证密码写入 SQL/YAML；鉴权配置仅存环境变量名。测试用的模拟字段和测试 URL 不用于线上。

H2 Oracle 模式不证明 DM8 驱动、CLOB、排序、锁、DIsql 建用户和表空间命令实机兼容。交付的真实 DM8 验收入口默认关闭，需要 DBA 在独立 YHT_MOCK_TEST Schema 演练；禁止直接指向业务库。

迁移要求先停全部旧实例并只读保存原文件，再建表、导入、核对、启用新服务。普通启动会创建默认配置，因此必须先迁移再启动；拒绝覆盖是保护机制，不应通过删除现有表绕过。

本次未修复批次 PROC 自动推进逻辑；迁移保留状态，不伪造成功。数据库写锁只保障每次存储操作，不承诺整个异步业务流程跨实例串行。日志不再按5000条自动截断，需监控表空间容量。
