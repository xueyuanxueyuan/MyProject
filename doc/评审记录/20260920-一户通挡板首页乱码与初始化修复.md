# 一户通挡板首页乱码与初始化异常修复复核

日期：2026-09-20

## 问题及依据

- 首页中文发生错误转码，title、属性引号和多个闭合标签损坏；源码、既有 target/release JAR 内均存在损坏内容，截图中的异常标题与其吻合。
- 对照历史提交 5f7da49 的正常文本，仅恢复当前首页中对应的 158 行乱码和标签，未整体回退文件。
- 浏览器复核发现 base64Utf8 在两个分支递归调用自身，触发 Maximum call stack size exceeded，阻断 fillTemplate 及后续初始化；改为调用 btoa 编码已转换的字节字符串。

## 范围与保留内容

- 修改 index.html 和 app.js（后者仅修复两行递归调用）。
- 保留银行主动签约/解约、费项列表、动账通知配置、MOVEMENT 筛选及清除历史数据控件。
- 不修改业务接口、环境配置、发布包，不提交、不部署。

## 回归验证

在仓库根目录执行：

~~~powershell
node --test yht-mock-server/src/test/js/static-page.test.cjs
node --check yht-mock-server/src/main/resources/static/yht-mock/app.js
~~~

- 修复前：标题、编码及标签结构三项检查失败。
- HTML 修复后：四项页面检查通过；新增 Base64 两个分支检查均复现栈溢出。
- Base64 修复后：六项回归检查通过，覆盖空字符串、ASCII、中文和 emoji，分别验证 TextEncoder 和兼容分支。
- 本地 Edge 无头浏览器加载真实 HTML/CSS/JS，验证标题、8 个功能区、模板初始化、链路筛选、银行主动签约报文预览；无页面脚本异常。
- 浏览器接口响应使用本地测试桩，未发送真实交易，不代表后端或 K8s 联调通过。

## 限制与后续

- 工具链切换因权限策略被拒绝，未运行 Maven 构建、Java 测试或重新打包。
- 既有 target/release JAR 未更新，不能将其视为已修复版本。
- 后续需按项目打包发布流程重新构建并部署，之后验证线上页面和真实接口；本次不执行发布。
