# 第七十一阶段 app Recon 创建成功日志业务编号边界记录

## 1. 阶段目标

本阶段延续 app 层主动业务日志降敏治理，聚焦 `ReconController#createRecon` 成功日志。

当前风险点：接口响应仍需按业务契约返回 `dzbh`，但服务端主动成功日志不应重复输出对账编号。`dzbh` 可能承载可关联业务轨迹，应按敏感业务编号处理。

## 2. 变更范围

### 2.1 生产代码

- `prod/IdeaProjects/capinfo-gjj-busi-jshs-v3/capinfo-gjj-busi-jshs-app/src/main/java/cn/capinfo/gjj/app/controller/ReconController.java`

变更内容：

```java
log.info("对账创建成功");
```

替代原先输出：

```java
log.info("对账创建成功: dzbh={}", result.getDzbh());
```

### 2.2 测试代码

- `prod/IdeaProjects/capinfo-gjj-busi-jshs-v3/capinfo-gjj-busi-jshs-app/src/test/java/cn/capinfo/gjj/app/controller/ReconControllerMockMvcTest.java`

新增测试：

```java
createReconSuccessLogShouldNotContainSensitiveReconNo
```

测试策略：

- Mock `ReconApplicationService#createRecon` 返回 `dzbh=DZ-SECRET-711` 的对账模型。
- 使用 Logback `ListAppender<ILoggingEvent>` 捕获 `ReconController` 日志。
- 断言响应 JSON 仍返回 `$.data.dzbh=DZ-SECRET-711`，保持接口契约不变。
- 断言日志包含固定事件名 `对账创建成功`。
- 断言日志不包含 `DZ-SECRET-711`。

## 3. TDD 验证记录

### 3.1 RED

命令：

```bash
export JAVA_HOME=/home/source/.vfox/sdks/java@17.0.2+8
export MAVEN_HOME=/home/source/.vfox/sdks/maven@3.9.14
export PATH="$JAVA_HOME/bin:$MAVEN_HOME/bin:$PATH"
java -version
mvn -v
mvn -pl capinfo-gjj-busi-jshs-app -am -Dtest=ReconControllerMockMvcTest#createReconSuccessLogShouldNotContainSensitiveReconNo -Dsurefire.failIfNoSpecifiedTests=false test
```

证据摘要：

- Java：`openjdk version "17.0.2"`
- Maven：`Apache Maven 3.9.14`
- RED 日志：`ReconController -- 对账创建成功: dzbh=DZ-SECRET-711`
- RED 失败：`assertFalse(logs.contains("DZ-SECRET-711"))` 失败，`expected: <false> but was: <true>`
- Maven 结果：`BUILD FAILURE`

### 3.2 GREEN 与组合回归

命令：

```bash
mvn -pl capinfo-gjj-busi-jshs-app -am -Dtest=ReconControllerMockMvcTest#createReconSuccessLogShouldNotContainSensitiveReconNo -Dsurefire.failIfNoSpecifiedTests=false test
mvn -pl capinfo-gjj-busi-jshs-app -am -Dtest=ReconControllerMockMvcTest,ReconControllerTest -Dsurefire.failIfNoSpecifiedTests=false test
```

证据摘要：

- GREEN 日志：`ReconController -- 对账创建成功`
- 定向测试：`Tests run: 1, Failures: 0, Errors: 0, Skipped: 0`
- Recon 组合测试：`Tests run: 4, Failures: 0, Errors: 0, Skipped: 0`
- Maven 结果：`BUILD SUCCESS`

### 3.3 app reactor 回归

命令：

```bash
mvn -pl capinfo-gjj-busi-jshs-app -am test
```

证据摘要：

- common：`Tests run: 5, Failures: 0, Errors: 0, Skipped: 0`
- domain：`Tests run: 54, Failures: 0, Errors: 0, Skipped: 0`
- infrastructure：`Tests run: 17, Failures: 0, Errors: 0, Skipped: 0`
- app：`Tests run: 105, Failures: 0, Errors: 0, Skipped: 0`
- Maven 结果：`BUILD SUCCESS`

## 4. 交付门禁

### 4.1 空白检查

命令：

```bash
git diff --check -- \
  prod/IdeaProjects/capinfo-gjj-busi-jshs-v3/capinfo-gjj-busi-jshs-app/src/main/java/cn/capinfo/gjj/app/controller/ReconController.java \
  prod/IdeaProjects/capinfo-gjj-busi-jshs-v3/capinfo-gjj-busi-jshs-app/src/test/java/cn/capinfo/gjj/app/controller/ReconControllerMockMvcTest.java
```

结果：退出码 0，未发现空白错误。

### 4.2 路径检查

检查结果：

- `ReconController.java` 位于 v3 app Maven 源码树，且非 `target` 下唯一匹配源文件。
- `ReconControllerMockMvcTest.java` 位于 v3 app Maven 测试源码树，且非 `target` 下唯一匹配测试文件。

### 4.3 Guardrail

命令：

```bash
scripts/guardrails/agent-delivery-guardrail.sh --scan-only \
  prod/IdeaProjects/capinfo-gjj-busi-jshs-v3/capinfo-gjj-busi-jshs-app/src/main/java/cn/capinfo/gjj/app/controller/ReconController.java \
  prod/IdeaProjects/capinfo-gjj-busi-jshs-v3/capinfo-gjj-busi-jshs-app/src/test/java/cn/capinfo/gjj/app/controller/ReconControllerMockMvcTest.java
```

结果：`✅ [通过] 未发现偷懒式占位痕迹。`

## 5. 风险边界

- 本阶段只修改本地 Java 日志模板与本地测试。
- 未修改接口路径、请求结构、响应结构、领域模型、持久化模型、Mapper SQL 或数据库设计。
- 未执行发布、远程上传、服务重启、生产配置变更或数据库变更。
- `$.data.dzbh` 响应字段保留，满足外部调用契约；仅收敛服务端主动日志输出。

## 6. R71-3 回归门禁复核

复核时间：2026-06-02 13:05（本地 Hermes 看板任务 `t_1b183d56`）。

### 6.1 工具链验证

命令：

```bash
export JAVA_HOME=/home/source/.vfox/sdks/java@17.0.2+8
export MAVEN_HOME=/home/source/.vfox/sdks/maven@3.9.14
export PATH="$JAVA_HOME/bin:$MAVEN_HOME/bin:$PATH"
java -version
mvn -v
```

结果：

- Java：`openjdk version "17.0.2" 2022-01-18`
- Maven：`Apache Maven 3.9.14`
- Maven 运行时 Java：`Java version: 17.0.2`

### 6.2 app reactor 回归

命令：

```bash
mvn -pl capinfo-gjj-busi-jshs-app -am clean test
```

结果：`BUILD SUCCESS`，各模块测试汇总如下：

| 模块 | 结果 |
|------|------|
| common | `Tests run: 5, Failures: 0, Errors: 0, Skipped: 0` |
| api | `No tests to run` |
| domain | `Tests run: 54, Failures: 0, Errors: 0, Skipped: 0` |
| infrastructure | `Tests run: 17, Failures: 0, Errors: 0, Skipped: 0` |
| app | `Tests run: 105, Failures: 0, Errors: 0, Skipped: 0` |

补充说明：先执行未带 `clean` 的 `mvn -pl capinfo-gjj-busi-jshs-app -am test` 时，Surefire 扫描历史 `target` 测试类出现 `ClassNotFoundException`（先后涉及 `YwglFallbackFactoryTest`、`PackageNamingConventionTest`）。执行 `clean test` 清理并重编译后复跑通过，判断为本地历史 target/classpath 残留导致的非代码逻辑失败。

### 6.3 git diff --check

命令：

```bash
git diff --check -- \
  prod/IdeaProjects/capinfo-gjj-busi-jshs-v3/capinfo-gjj-busi-jshs-app/src/main/java/cn/capinfo/gjj/app/controller/ReconController.java \
  prod/IdeaProjects/capinfo-gjj-busi-jshs-v3/capinfo-gjj-busi-jshs-app/src/test/java/cn/capinfo/gjj/app/controller/ReconControllerMockMvcTest.java \
  doc/设计文档/20260602-第七十一阶段app-recon创建成功日志业务编号边界记录.md
```

结果：退出码 0，未发现空白错误。

### 6.4 路径检查

检查结果：

- `ReconController.java` 存在于 v3 app Maven 主源码树，排除 `target` 后文件系统唯一匹配数为 1。
- `ReconControllerMockMvcTest.java` 存在于 v3 app Maven 测试源码树，排除 `target` 后文件系统唯一匹配数为 1。
- 本阶段记录文件存在于 `doc/设计文档`。

### 6.5 Guardrail

命令：

```bash
scripts/guardrails/agent-delivery-guardrail.sh --scan-only \
  prod/IdeaProjects/capinfo-gjj-busi-jshs-v3/capinfo-gjj-busi-jshs-app/src/main/java/cn/capinfo/gjj/app/controller/ReconController.java \
  prod/IdeaProjects/capinfo-gjj-busi-jshs-v3/capinfo-gjj-busi-jshs-app/src/test/java/cn/capinfo/gjj/app/controller/ReconControllerMockMvcTest.java \
  doc/设计文档/20260602-第七十一阶段app-recon创建成功日志业务编号边界记录.md
```

结果：`✅ [通过] 未发现偷懒式占位痕迹。`

## 7. 下一步建议

继续按低风险本地阶段推进 app 主动业务日志治理。当前 app reactor 日志中仍可见部分 Controller 主动成功日志输出业务编号或地区编码，例如：

- `VoucherController -- 凭证创建成功: pzbh=<凭证编号>`
- `AccountController -- 账户注册成功: zhbh=<账户编号>`
- `AccountController -- 查询账户列表: hsdwdm=<核算单位代码>, count=<结果数量>`

建议第七十二阶段优先选择 `VoucherController#createVoucher` 成功日志切片，按同样 TDD 模式新增哨兵测试、确认 RED、最小收敛日志、运行 app reactor 与 guardrail。
