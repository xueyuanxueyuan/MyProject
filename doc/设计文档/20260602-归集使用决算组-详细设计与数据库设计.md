# 资金预决算及执行情况 - 归集使用决算组详细设计与数据库设计

## 1. 设计目标

本文档用于把“归集使用决算组”推进到可直接指导后端开发、前端联调和达梦建表的详细设计层级。

本组范围仅包含：

- 住房公积金归集使用决算总表
- 住房公积金归集使用决算附表

本组关联文档：

- [归集使用决算组设计.md](file:///home/source/Jetbrains/Probject/Gjj/doc/设计文档/20260602-归集使用决算组设计.md)

## 2. 本轮定版结论

本轮按已确认口径，将归集使用决算组结构定为：

1. 附表承接机构决算来源结果，是总表的唯一实绩来源。
2. 总表采用“主表 + 总表明细行”模式。
3. 附表继续采用“一张报表 + 一个机构一行”的固定字段模式。
4. 总表、总表明细、附表明细统一使用 15 个系统字段。
5. 总表与附表统一使用 `MAIN_ID + BBBH` 双关联。
6. 本组不开放常规金额手工录入，只允许同步来源和确认流转。
7. 总表接口返回 15 行 `details` 数组，附表接口返回机构明细数组。

## 3. 业务对象设计

## 3.1 总表主表对象

表名：`CWHS_ZJYJS_GJSYJSZB`

定位：

- 一张年度归集使用决算总表一行
- 存表头、状态、同步时间、审核、锁定等控制信息
- 不直接存 15 行业务值

职责：

- 作为归集使用决算主单
- 作为总表明细和附表明细的父记录
- 作为状态流转、审核和锁定控制主体

## 3.2 总表明细对象

表名：`CWHS_ZJYJS_GJSYJSZMX`

定位：

- 一张总表的 15 行业务项逐行存储
- 一行代表 Excel 总表中的一个项目
- 存该行的 `SNTQS`、`BNYSS`、`BNSJS`、`YSWCL`、`BSTQZZ`

职责：

- 直接承载总表展示数据
- 直接支撑总表前端与 Excel 导出
- 避免总表主表字段膨胀

## 3.3 附表明细对象

表名：`CWHS_ZJYJS_GJSYJSFB`

定位：

- 一张附表按机构拆成多行
- 一行代表一个核算机构在一个决算单中的全部固定字段
- 是机构来源展示、总表实绩汇总和流转确认的基础

职责：

- 存储机构来源结果
- 存储机构公式计算结果
- 存储机构同步状态与流转状态
- 为总表明细提供全市实绩来源

## 4. 状态流转设计

统一状态建议：

- `0`：草稿
- `1`：已提交
- `2`：审核退回
- `3`：审核通过
- `4`：已锁定

流转规则：

1. 市中心创建归集使用决算主单。
2. 系统初始化总表 15 行明细和附表机构行。
3. 系统执行来源同步，抓取上年决算、本年预算和 12 月执行情况。
4. 分中心查看本机构附表结果并提交确认。
5. 市中心审核通过或退回。
6. 审核通过后，市中心执行锁定。
7. 锁定后总表主表、总表明细、附表明细全部只读。

## 5. 系统字段口径

三张表统一保留以下 15 个系统字段：

- `ID BIGINT NOT NULL`
- `ZXBH NVARCHAR(15) NOT NULL`
- `REVISION INT NOT NULL`
- `CREATOR NVARCHAR(100)`
- `CREATED_TIME TIMESTAMP(0) NOT NULL DEFAULT CURRENT_TIMESTAMP()`
- `UPDATOR NVARCHAR(100)`
- `UPDATED_TIME TIMESTAMP(0)`
- `JBJGBH NVARCHAR(30) NOT NULL`
- `JBJGMC NVARCHAR(90) NOT NULL`
- `WDBH NVARCHAR(30)`
- `WDMC NVARCHAR(90)`
- `QDLX NVARCHAR(10)`
- `QDBM NVARCHAR(16)`
- `DEL_FLAG NVARCHAR(1) NOT NULL DEFAULT '0'`
- `HSJGBH NVARCHAR(10) NOT NULL DEFAULT '01'`

说明：

- 主表中的 `HSJGBH` 保存全市口径，如 `01`
- 总表明细中的 `HSJGBH` 与主表保持一致
- 附表明细中的 `HSJGBH` 保存具体确认机构

## 6. 字段类型规则

金额字段统一使用：

- `DECIMAL(20,4)`

比例字段统一使用：

- `DECIMAL(20,6)`

编号类字段统一使用：

- `VARCHAR2` 或 `NVARCHAR`

## 7. 数据库设计

## 7.1 总表主表

表名：`CWHS_ZJYJS_GJSYJSZB`

### 业务字段

- `BBBH VARCHAR2(50)` 报表编号
- `ND VARCHAR2(4)` 年度
- `BZDW VARCHAR2(100)` 编制单位
- `BBZT VARCHAR2(2)` 报表状态
- `QSSJ TIMESTAMP(0)` 最近同步时间
- `DWFZR VARCHAR2(50)` 单位负责人
- `KJZG VARCHAR2(50)` 会计主管
- `ZBR VARCHAR2(50)` 制表人
- `SHYJ VARCHAR2(500)` 审核意见
- `SPSJ TIMESTAMP(0)` 审批时间
- `SFSD CHAR(1)` 是否锁定
- `SDSJ TIMESTAMP(0)` 锁定时间

### 索引建议

- `IDX_CWHS_ZJYJS_GJSYJSZB_ND (ND, DEL_FLAG)`
- `IDX_CWHS_ZJYJS_GJSYJSZB_BBBH (BBBH)`

## 7.2 总表明细表

表名：`CWHS_ZJYJS_GJSYJSZMX`

### 关联字段

- `MAIN_ID BIGINT` 主表ID
- `BBBH VARCHAR2(50)` 报表编号
- `ND VARCHAR2(4)` 年度

### 结构字段

- `HANGXH INT` 行序号
- `FLMC VARCHAR2(50)` 分类名称
- `XMBM VARCHAR2(50)` 项目编码
- `XMMC VARCHAR2(100)` 项目名称
- `SFGS CHAR(1)` 是否公式行
- `SFBSY CHAR(1)` 是否不适用行

### 值字段

- `SNTQS DECIMAL(20,4)` 上年同期数
- `BNYSS DECIMAL(20,4)` 本年预算数
- `BNSJS DECIMAL(20,4)` 本年实绩数
- `YSWCL DECIMAL(20,6)` 预算完成率
- `BSTQZZ DECIMAL(20,6)` 比上年同期增长

### 索引建议

- `IDX_CWHS_ZJYJS_GJSYJSZMX_MAIN (MAIN_ID, HANGXH)`
- `IDX_CWHS_ZJYJS_GJSYJSZMX_BBBH (BBBH, XMBM)`

## 7.3 附表明细表

表名：`CWHS_ZJYJS_GJSYJSFB`

### 关联字段

- `MAIN_ID BIGINT` 主表ID
- `BBBH VARCHAR2(50)` 报表编号
- `ND VARCHAR2(4)` 年度
- `BBZT VARCHAR2(2)` 行状态
- `SFSD CHAR(1)` 是否锁定
- `QSZT VARCHAR2(2)` 来源同步状态
- `QSCWYY VARCHAR2(500)` 同步错误原因
- `TJSJ TIMESTAMP(0)` 提交时间

### 固定业务字段

#### 归集提取

- `GJTQ_NCYE DECIMAL(20,4)` 归集提取年初余额
- `GJTQ_DNGJS DECIMAL(20,4)` 归集提取当年归集数
- `GJTQ_DNTQS DECIMAL(20,4)` 归集提取当年提取数
- `GJTQ_DNTCDJZJ DECIMAL(20,4)` 归集提取当年统筹调剂资金
- `GJTQ_NMYE DECIMAL(20,4)` 归集提取年末余额

#### 个人贷款

- `GRDK_NCYE DECIMAL(20,4)` 个人贷款年初余额
- `GRDK_DNFFS DECIMAL(20,4)` 个人贷款当年发放数
- `GRDK_DNSHS DECIMAL(20,4)` 个人贷款当年收回数
- `GRDK_DNGZSDKZC DECIMAL(20,4)` 个人贷款当年公转商贷款转出
- `GRDK_NMYE DECIMAL(20,4)` 个人贷款年末余额
- `GRDK_DKYEZBGJYE DECIMAL(20,6)` 个人贷款余额占归集余额比

#### 国债投资

- `GDTZ_NCYE DECIMAL(20,4)` 国债投资年初余额
- `GDTZ_DNZJS DECIMAL(20,4)` 国债投资当年增加数
- `GDTZ_DNHSS DECIMAL(20,4)` 国债投资当年回收数
- `GDTZ_NMYE DECIMAL(20,4)` 国债投资年末余额
- `GDTZ_YEZBGJYE DECIMAL(20,6)` 国债投资余额占归集余额比

### 索引建议

- `IDX_CWHS_ZJYJS_GJSYJSFB_MAIN_HSJG (MAIN_ID, HSJGBH)`
- `IDX_CWHS_ZJYJS_GJSYJSFB_BBBH_HSJG (BBBH, HSJGBH, DEL_FLAG)`

## 8. 总表 15 行定义

总表明细固定初始化以下 15 行：

1. `GJTQ_NCYE` 归集提取-年初余额
2. `GJTQ_DNGJS` 归集提取-当年归集数
3. `GJTQ_DNTQS` 归集提取-当年提取数
4. `GJTQ_NMYE` 归集提取-年末余额
5. `GRDK_NCYE` 个人贷款-年初余额
6. `GRDK_DNFFS` 个人贷款-当年发放数
7. `GRDK_DNSHS` 个人贷款-当年收回数
8. `GRDK_DNGZSDKZC` 个人贷款-当年公转商贷款转出
9. `GRDK_NMYE` 个人贷款-年末余额
10. `GRDK_DKYEZBGJYE` 个人贷款余额占归集余额比%
11. `GDTZ_NCYE` 国债投资-年初余额
12. `GDTZ_DNZJS` 国债投资-当年增加数
13. `GDTZ_DNHSS` 国债投资-当年回收数
14. `GDTZ_NMYE` 国债投资-年末余额
15. `GDTZ_YEZBGJYE` 国债投资余额占归集余额比%

## 9. 字段编辑控制

## 9.1 总表

总表所有字段均只读。

## 9.2 附表

附表业务字段在本组中均只读。

分中心可执行的仅是：

- 来源同步
- 提交确认

## 10. 计算规则设计

## 10.1 附表计算

```java
void recalcDetail(DecisionFbDetail detail) {
    detail.setGjtqNmye(nvl(detail.getGjtqNcye())
        + nvl(detail.getGjtqDngjs())
        - nvl(detail.getGjtqDntqs())
        + nvl(detail.getGjtqDntcdjzj()));

    detail.setGrdkNmye(nvl(detail.getGrdkNcye())
        + nvl(detail.getGrdkDnffs())
        - nvl(detail.getGrdkDnshs())
        - nvl(detail.getGrdkDngzsdkzc()));

    detail.setGrdkDkyezbgjye(calcRatio(detail.getGrdkNmye(), detail.getGjtqNmye()));

    detail.setGdtzNmye(nvl(detail.getGdtzNcye())
        + nvl(detail.getGdtzDnzjs())
        - nvl(detail.getGdtzDnhss()));

    detail.setGdtzYezbgjye(calcRatio(detail.getGdtzNmye(), detail.getGjtqNmye()));
}
```

## 10.2 总表生成

```java
void syncTotalDetails(Long mainId) {
    Map<String, BigDecimal> actualMap = sumFbFields(mainId);
    Map<String, BigDecimal> budgetMap = loadBudgetValues(mainId);
    Map<String, BigDecimal> lastYearMap = loadLastYearDecisionValues(mainId);

    for (DecisionZbDetail row : listZbDetails(mainId)) {
        row.setSntqs(lastYearMap.get(row.getXmbm()));
        row.setBnyss(budgetMap.get(row.getXmbm()));
        row.setBnsjs(actualMap.get(row.getXmbm()));

        if (isRatioApplicable(row.getXmbm())) {
            row.setYswcl(calcRatio(row.getBnsjs(), row.getBnyss()));
            row.setBstqzz(calcGrowth(row.getBnsjs(), row.getSntqs()));
        } else {
            row.setYswcl(null);
            row.setBstqzz(null);
        }

        updateZbDetail(row);
    }
}
```

## 10.3 同步来源流程

```java
DecisionMain syncAll(String nd, String operator) {
    DecisionMain main = getOrCreateMain(nd, operator);

    List<DecisionFbDetail> details = buildOrLoadFbDetails(main);
    Map<String, ExecutionSnapshot> execMap = loadDecemberExecution(nd);
    Map<String, BudgetSnapshot> budgetMap = loadBudgetSnapshot(nd);
    Map<String, LastYearDecisionSnapshot> lastYearMap = loadLastYearDecision(nd);

    for (DecisionFbDetail detail : details) {
        fillFromBudget(detail, budgetMap.get(detail.getHsjgbh()));
        fillFromExecution(detail, execMap.get(detail.getHsjgbh()));
        fillFromLastYear(detail, lastYearMap.get(detail.getHsjgbh()));
        recalcDetail(detail);
        detail.setQszt("1");
        updateFbDetail(detail, operator);
    }

    syncTotalDetails(main.getId());
    main.setQssj(now());
    updateMain(main, operator);
    return main;
}
```

## 11. 数据流转设计

1. 市中心创建归集使用决算主单。
2. 系统初始化总表 15 行和附表机构行。
3. 系统同步来源：预算、执行、上年决算。
4. 分中心查看本机构附表结果并提交确认。
5. 市中心审核通过或退回。
6. 锁定后生成正式快照与导出文件。

## 12. 接口设计建议

## 12.1 查询总表

`GET /gjsyjs/zb/{mainId}`

返回：

- `main`
- `details`

## 12.2 查询附表

`GET /gjsyjs/fb/{mainId}`

返回：

- `main`
- `details`

## 12.3 同步来源

`POST /gjsyjs/sync`

入参：

- `mainId`
- `scope`：`all` / `currentOrg`

## 12.4 提交确认

`POST /gjsyjs/submit`

入参：

- `mainId`
- `hsjgbh`

## 12.5 审核、退回、锁定

- `POST /gjsyjs/approve`
- `POST /gjsyjs/reject`
- `POST /gjsyjs/lock`

## 13. 前端交互实现建议

1. 总表页签直接消费 `gjsyjsZbData.details`
2. 附表页签消费 `gjsyjsFbData.details`，前端透视为“全市 + 各机构列”
3. 分中心默认进入附表页签，市中心默认进入总表页签
4. 余额类、比率类和国债投资行按配置控制显示 `-` 或 `0`
5. 所有业务单元格只读，页面按钮驱动同步与确认流转

## 14. 本轮结论

本轮详细设计的核心结论如下：

1. 归集使用决算组采用 3 张业务表：主表、总表明细表、附表明细表。
2. 总表明细承接 15 行决算指标，附表明细承接 16 行机构结果。
3. 本组数据全部来源于预算、执行和上年决算，不允许在本表中人工改写业务值。
4. 分中心承担结果确认职责，市中心承担审核、锁定和导出职责。
