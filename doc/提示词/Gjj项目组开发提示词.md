# Gjj 项目组开发提示词

## 1) 新增后端接口（通用）
你现在是 Gjj 项目组后端开发工程师，请基于现有代码风格实现新接口。

要求：
0. **强制以 SDD（软件设计文档）模式进行**：在编写任何代码之前，必须先产出并确认核心设计（如接口定义、数据结构、架构流转），然后再进行编码。
1. 保持分层：`api -> controller -> service -> dm -> mapper/xml`。
2. 返回统一 `R<T>`，入参与出参使用 DTO。
3. Controller 补充操作日志与接口注解（如 `@OptLog`、`@Operation`）。
4. 入参按项目规范做校验（如 `@Check`）。
5. 涉及数据库：
   - 简单查询用 MyBatis-Plus；
   - 复杂分页/联表用 XML。
6. 涉及 Feign：
   - 提供降级；
   - 降级不得 `return null`。
7. 输出：修改文件清单、核心逻辑说明、验证步骤。

需求如下：
【填写具体需求】

---

## 2) 缺陷修复（定位 + 修复）
请排查并修复以下问题（Gjj 项目）：
【贴现象/日志/报错】

输出格式：
1. 根因定位（在哪一层：controller/service/sql/feign/job）。
2. 修复方案（最小改动）。
3. 风险评估（兼容性、性能、状态一致性）。
4. 回归用例（成功/失败/边界）。

---

## 3) SQL 优化（分页与性能）
请优化以下 SQL/Mapper 方法：
【填 SQL 或 mapper 方法】

要求：
1. 业务语义不变，返回结构不变。
2. 优化 where、join、分页排序策略。
3. 提供优化前后对比与原因。
4. 给出索引建议（如需要）。
5. 提供验证方式（慢查询、分页深度、数据量场景）。

---

## 4) Feign 降级治理（专项）
请治理当前模块 Feign 降级代码，重点处理 `return null`。

要求：
0. **强制以 SDD（软件设计文档）模式进行**：在进行任何重构或大面积修改之前，必须先明确治理设计方案。
1. 不改接口签名。
2. 降级逻辑必须可观测：日志 + `R.fail(...)` / `BizException`。
3. 优先处理交易发起、结果查询、记账通知相关接口。
4. 输出修复清单与剩余风险清单。

---

## 5) 代码评审（项目规范视角）
请对本次改动做代码评审，重点关注：
0. **是否遵循 SDD 模式**：是否有前置的设计文档支撑当前代码变更。
1. 分层是否清晰，Controller 是否过重。
2. 是否存在空降级、吞异常、无日志失败分支。
3. 交易/审批状态流转是否完整且可追踪。
4. SQL 是否存在全表扫描风险。
5. 外部调用失败是否可观测且可恢复。

请按严重级别输出：`致命` / `高` / `中` / `建议`。

---

## 6) 提交前自检
请按以下清单检查当前变更并给出“通过/不通过”：
- 是否已在修改前完成 SDD（软件设计文档）模式的规划与确认；
- 编译是否通过；
- 返回是否统一 `R<T>`；
- 校验、日志、注解是否齐全；
- Feign 降级是否无 `return null`；
- SQL 是否具备分页与合理筛选；
- 是否包含最小回归验证步骤。
