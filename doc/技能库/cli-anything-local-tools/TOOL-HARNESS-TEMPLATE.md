# TOOL HARNESS TEMPLATE

## 1. 工具基本信息

- 工具名称：
- 可执行文件名：
- 典型安装路径：
- 调用方式：
  - 原生 CLI / 无头模式 / 脚本接口 / 本地 API / GUI 自动化

## 2. 探测命令

```powershell
Get-Command <tool>
where.exe <tool>
<tool> --version
<tool> --help
```

若不是 PATH 工具，请记录绝对路径探测方式。

## 3. 只读能力

- 查看版本：
- 查看状态：
- 查看帮助：
- 列出现有对象 / 工程 / 资源：

## 4. 最小可用调用

列出一个副作用最小的验证命令，例如：

```powershell
<tool> <safe-subcommand> --help
```

或：

```powershell
<tool> <read-only-action> --json
```

## 5. 常见任务映射

| 用户意图 | 推荐命令 | 输出校验 |
|---|---|---|
| 查询信息 |  |  |
| 导出结果 |  |  |
| 修改对象 |  |  |
| 批处理 |  |  |

## 6. 输出与验证

- 退出码预期：
- 成功输出关键字：
- 失败输出关键字：
- 产物文件路径检查：
- 二次验证命令：

## 7. 风险等级

- 只读
- 项目内写操作
- 系统级写操作
- 需要管理员权限
- 需要联网

## 8. 对代理的专用约束

- 是否必须先读取帮助再调用：
- 是否必须先创建临时目录：
- 是否要求绝对路径：
- 是否支持 JSON 输出：
- 是否存在长驻进程：
- 是否需要用户额外审批：

## 9. 已知问题与回退方案

- 不在 PATH 时怎么办：
- 帮助命令失败时怎么办：
- GUI 必须打开时怎么办：
- 无头模式不可用时怎么办：

## 10. 建议补充文件

- 示例命令清单
- 常见错误速查
- 真实调用样例
