# 20260623-枣庄总账明细账后端二次提交推送记录

## 1. 任务背景

- 在首次提交总账、明细账相关后端改动后，`CwhsServiceImpl.java` 中还保留了两处未提交修改。
- 本次补充提交的目标是将显示凭证号格式长度从写死常量改为读取配置项，避免枣庄环境仍固定按 4 位长度生成。

## 2. 变更文件

- 仓库：`zaozhuang/IdeaProjects/capinfo-gjj-busi-jshs`
- 文件：
  - `capinfo-gjj-busi-cwhs-jzgl/capinfo-gjj-busi-cwhs-jzgl-basic-svc-busi/src/main/java/cn/capinfo/gjj/busi/cwhs/jzgl/busi/service/impl/CwhsServiceImpl.java`

## 3. 变更内容

- 将以下两处显示凭证号格式由固定长度 `4` 收口为配置值 `ywcsConfig.getXspzhLength()`：
  - 汇总凭证生成逻辑处
  - 账表凭证汇总生成逻辑处

## 4. 提交前校验

- 远端同步状态：

```bash
git fetch origin
git rev-list --left-right --count HEAD...origin/zaozhuang/dev
```

- 结果：
  - 本地领先：`0`
  - 本地落后：`0`

- 构建验证命令：

```bash
export JAVA_HOME="/home/source/.vfox/cache/java/v-17.0.2+8/java-17.0.2+8"
export PATH="${JAVA_HOME}/bin:/home/source/.vfox/cache/maven/v-3.9.14/maven-3.9.14/bin:$PATH"
mvn -pl capinfo-gjj-busi-cwhs-jzgl/capinfo-gjj-busi-cwhs-jzgl-basic-svc-app -am -DskipTests -s settings-tsp.xml package
```

- 结果：
  - `BUILD SUCCESS`

## 5. 提交信息

- Commit：`1b52caacf`
- Message：

```text
fix: 按配置收口枣庄显示凭证号长度

将 CwhsServiceImpl 中两处显示凭证号格式统一改为按 ywcsConfig.getXspzhLength() 取值，避免仍写死 4 位长度。

AI辅助: true
AI工具: Trae
```

## 6. 推送结果

- 分支：`zaozhuang/dev`
- 推送结果：成功
- 远端更新：

```text
7d94d83e0..1b52caacf
```

## 7. 结论

- `CwhsServiceImpl.java` 剩余后端修改已完成再次提交并推送。
- 当前该次补充改动已经进入远端 `origin/zaozhuang/dev`，可继续用于后续联调或发版。
