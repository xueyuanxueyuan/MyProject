# 20260512-枣庄-凭证汇总改造-V1.1-第四笔原子提交记录

## 1. 目标
- 对齐详细设计 V1.1 的“配置与灰度”要求。
- 仅补充枣庄区域配置中与本功能直接相关的 `voucher` 开关。
- 不混入原始脏仓里其他环境地址、Nacos、命名空间等无关配置调整。

## 2. 本次变更文件
- `capinfo-gjj-busi-cwhs-jzgl-basic-svc-app/src/main/resources/application-city-zaozhuang.yml`

## 3. 变更内容
- 新增 `voucher.summary.byYwfl: true`
- 新增 `voucher.mapping.strictMode: false`
- 新增 `voucher.mapping.fallback.enable: true`

## 4. 提交边界
- 本笔只做区域灰度开关配置。
- 未提交 `application.yml`，因为原始脏仓中的该文件差异混有大量环境改造，不属于本次凭证汇总功能范围。
- 未提交其他系统模块下的 `application*.yml` 改动，避免污染功能分支。

## 5. 验证
- 使用 `grep` 复核 `application-city-zaozhuang.yml` 中已存在 3 个目标配置项。
- 执行 `scripts/guardrails/agent-delivery-guardrail.sh --scan-only`，结果通过。

## 6. 说明
- 该提交适合作为设计任务 5/6 之间的独立原子提交：先让代码口径落地，再让区域配置显式启用。
