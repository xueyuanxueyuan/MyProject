# 归集使用决算组设计与原型脚本记录

## 1. 本轮目标

在完成扩面工作完成情况组后，继续进入下一个功能组：

- 归集使用决算组

本轮目标是一次性补齐：

- 功能设计文档
- 详细设计与数据库设计文档
- 独立达梦执行脚本
- 原型改造

## 2. 本轮依据

本轮主要依据：

- Excel《住房公积金归集使用决算总表》
- Excel《住房公积金归集使用决算附表》
- 共性规则组设计
- 分功能设计计划
- 当前交互原型中 `gjsyjs` 菜单的现状实现

## 3. 本轮确认的关键规则

1. 本组是年度双表，不是单表。
2. 总表按 15 行展示，附表按 16 行展示。
3. 总表的核心列是：上年同期数、本年预算数、本年实绩数、预算完成率、比上年同期增长。
4. 附表的核心列是：全市 + 各分中心本年实绩数。
5. 本组本期不新增常规手工录入值，主要承接预算、执行情况和上年决算的自动生成结果。
6. 分中心负责确认本机构附表结果，市中心负责审核、退回、锁定和导出。

## 4. 本轮产出文件

- [归集使用决算组设计.md](file:///home/source/Jetbrains/Probject/Gjj/doc/设计文档/20260602-归集使用决算组设计.md)
- [归集使用决算组-详细设计与数据库设计.md](file:///home/source/Jetbrains/Probject/Gjj/doc/设计文档/20260602-归集使用决算组-详细设计与数据库设计.md)
- [归集使用决算组-达梦执行脚本.sql](file:///home/source/Jetbrains/Probject/Gjj/doc/数据库脚本/20260602-归集使用决算组-达梦执行脚本.sql)
- [index.html](file:///home/source/Jetbrains/Probject/Gjj/doc/操作记录/20260601-资金预决算及执行情况-交互原型/index.html)

## 5. 本轮原型收口

本轮继续对 `index.html` 中的 `gjsyjs` 菜单做了专属改造收口，已完成以下内容：

- 补齐归集使用决算总表 / 附表双页签切换。
- 补齐分中心视角下的“同步来源、提交确认”按钮链路。
- 补齐市中心视角下的“审核通过、退回、锁定、导出”按钮链路。
- 固化附表 16 行、总表 15 行的专属渲染与汇总逻辑。
- 修正按钮模板分支，避免分中心视角出现与通用逻辑重复渲染的按钮。
- 清理 `handleGjsyjsFetch` 中大小写不一致的无效字段写法。
- 在页面初始化时补充 `refreshGjsyjsView()`，避免首次进入菜单时行数据未构建。

## 6. 验证结果

### 6.1 静态诊断

- `index.html` 已执行 `GetDiagnostics`
- 结果：无诊断错误

### 6.2 浏览器验收

基于本地 HTTP 预览对 `gjsyjs` 页面完成了实际浏览器验收，结论如下：

1. 切换到 `归集使用决算` 菜单后，标题、页签和专属数据视图可正常加载。
2. 市中心视角默认进入总表页签，可见“审核通过 / 退回 / 锁定 / 导出完整报表”按钮。
3. 分中心视角切换后默认进入附表页签，仅展示本机构列，按钮为“同步来源 / 提交确认”，未再出现重复审核按钮。
4. 执行“同步来源”后，最近取数时间与来源值会刷新，总表汇总值同步更新。
5. 执行“提交确认”后，分中心明细状态会更新；切回市中心后可继续执行审核通过和锁定。
6. 执行“审核通过”“锁定”后，主表状态和附表机构状态能同步变化。
7. 浏览器控制台无页面脚本错误，只有 Vue 开发模式提示信息。

### 6.3 当前结论

- 归集使用决算组文档、脚本、原型已形成闭环。
- 本组已可作为客户演示和后续开发说明的交互原型基础版本。
- 后续可按既定顺序进入下一个功能组：业务收支决算组。
