# 20260521-枣庄测试联调发布与生产上传记录

## 1. 任务目标

- 更新枣庄项目代码。
- 打包枣庄前后端产物。
- 发布到枣庄测试环境 `zaozhuang:test`。
- 发布到住建部联调环境 `zaozhuang:test-zjb`。
- 将同批次制品上传到枣庄生产环境 `zaozhuang:prod`，不执行生产重启。

## 2. 执行前确认

### 2.1 规范与入口

- 参考 `doc/项目规范/打包编译发布规范.md`。
- 参考 `doc/技能库/gjj-build-deploy/SKILL.md`。
- 参考 `doc/技能库/gjj-remote-deploy/SKILL.md`。
- 参考 `doc/技能库/vfox-toolchain/SKILL.md`。

### 2.2 本地工作区状态

- 后端目录：`zaozhuang/IdeaProjects/capinfo-gjj-busi-jshs`
- 前端目录：`zaozhuang/WebstormProjects/capinfo-gjj-frontend-jshs-gm`
- 发布目录：`zaozhuang/release`
- 后端分支：`zaozhuang/dev`
- 前端分支：`zaozhuang/dev`
- 后端在执行前已存在本地改动，`git pull` 返回“已经是最新的”。
- 前端在执行前已存在本地改动，按用户指示继续 `git pull`，最终完成 `782c828b -> 5c2a2423` 的 fast-forward 更新。

## 3. 工具链与构建

### 3.1 后端打包

- 目录：`zaozhuang/IdeaProjects/capinfo-gjj-busi-jshs`
- 工具链：
  - `vfox use java@17.0.2+8`
  - `vfox use maven@3.9.14`
  - `eval "$(vfox env -s bash)"`
- 校验命令：
  - `java -version`
  - `mvn -v`
- 构建命令：
  - `mvn clean package -DskipTests -s settings-tsp.xml`
- 结果：`BUILD SUCCESS`

### 3.2 前端打包

- 目录：`zaozhuang/WebstormProjects/capinfo-gjj-frontend-jshs-gm`
- 工具链：
  - `vfox use nodejs@16.20.2`
  - `eval "$(vfox env -s bash)"`
- 校验命令：
  - `node -v`
  - `npm -v`
- 构建命令：
  - `rm -rf dist`
  - `npm install`
  - `npm run build`
  - `zip -rq capinfo-gjj-frontend-jshs-gm.zip dist`
- 结果：构建完成，存在 4 条 warning，但未阻塞产物生成。

### 3.3 制品聚合

- 执行了 `zaozhuang/release` 清空与重新归档。
- 最终聚合的 8 个制品如下：
  - `capinfo-gjj-busi-cwhs-jzgl-basic-svc-app.jar`
  - `capinfo-gjj-busi-jhgl-zjjh-basic-svc-app.jar`
  - `capinfo-gjj-busi-jshs-gm-agg-svc-app.jar`
  - `capinfo-gjj-busi-zjjs-lcgl-basic-svc-app.jar`
  - `capinfo-gjj-busi-zjjs-sp-app.jar`
  - `capinfo-gjj-busi-zjjs-ywgl-basic-svc-app.jar`
  - `capinfo-gjj-busi-zjjs-zhgl-basic-svc-app.jar`
  - `capinfo-gjj-frontend-jshs-gm.zip`

## 4. 远程发布与上传

### 4.1 住建部联调环境

- 目标环境：`zaozhuang:test-zjb`
- 上传命令：
  - `python3 scripts/remote_deploy.py zaozhuang:test-zjb`
- 上传结果：8 个制品全部上传成功。
- 重启命令：
  - `python3 scripts/remote_exec.py zaozhuang:test-zjb "kubectl rollout restart deployment ... -n {namespace}"`
- 首次状态校验时，因 shell 循环变量未转义导致 `rollout status` 命令拼接错误。
- 已立即修正并重新执行状态校验。
- 最终校验结果：以下 8 个 deployment 全部 `successfully rolled out`
  - `capinfo-gjj-busi-cwhs-jzgl-basic-svc-depl`
  - `capinfo-gjj-busi-jhgl-zjjh-basic-svc-depl`
  - `capinfo-gjj-busi-jshs-gm-agg-svc-depl`
  - `capinfo-gjj-busi-zjjs-lcgl-basic-svc-depl`
  - `capinfo-gjj-busi-zjjs-sp-basic-svc-depl`
  - `capinfo-gjj-busi-zjjs-ywgl-basic-svc-depl`
  - `capinfo-gjj-busi-zjjs-zhgl-basic-svc-depl`
  - `capinfo-gjj-frontend-jshs-gm-depl`

### 4.2 枣庄测试环境

- 目标环境：`zaozhuang:test`
- 上传命令：
  - `python3 scripts/remote_deploy.py zaozhuang:test`
- 上传结果：因与联调环境共享同主机同路径，命中 `remote_deploy.py` 去重缓存，跳过重复传包。
- 重启命令：
  - `python3 scripts/remote_exec.py zaozhuang:test "kubectl rollout restart deployment ... -n {namespace}"`
- 状态校验结果：以下 8 个 deployment 全部 `successfully rolled out`
  - `capinfo-gjj-busi-cwhs-jzgl-basic-svc-depl`
  - `capinfo-gjj-busi-jhgl-zjjh-basic-svc-depl`
  - `capinfo-gjj-busi-jshs-gm-agg-svc-depl`
  - `capinfo-gjj-busi-zjjs-lcgl-basic-svc-depl`
  - `capinfo-gjj-busi-zjjs-sp-basic-svc-depl`
  - `capinfo-gjj-busi-zjjs-ywgl-basic-svc-depl`
  - `capinfo-gjj-busi-zjjs-zhgl-basic-svc-depl`
  - `capinfo-gjj-frontend-jshs-gm-depl`

### 4.3 枣庄生产环境

- 目标环境：`zaozhuang:prod`
- 上传命令：
  - `python3 scripts/remote_deploy.py zaozhuang:prod`
- 上传结果：8 个制品全部上传成功。
- 说明：按本次指令，仅执行生产物理包上传，未执行任何生产重启命令。

## 5. 本次执行结论

- 枣庄后端打包成功。
- 枣庄前端打包成功。
- `zaozhuang/release` 已刷新为本次最新的 8 个制品。
- `zaozhuang:test-zjb` 已完成上传、重启和 rollout 校验。
- `zaozhuang:test` 已完成重启和 rollout 校验，上传阶段命中去重缓存。
- `zaozhuang:prod` 已完成制品上传，未重启。

## 6. 后续建议

- 联调团队可优先在 `zaozhuang:test-zjb` 进行业务联调验证。
- 测试团队可在 `zaozhuang:test` 执行本轮改动的业务回归。
- 若后续需要正式切换生产版本，再单独执行生产重启并补一份生产变更记录。
