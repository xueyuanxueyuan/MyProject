# 20260706-链接服务器配置文件创建记录

## 1. 操作目标

创建远程发布脚本使用的链接服务器配置文件，用于枣庄测试环境发布链路。

## 2. 配置文件

- 文件路径：`config/deploy_servers.json`
- 配置用途：供 `remote_deploy.py`、`remote_exec.py` 读取服务器连接、远程目录、命名空间和部署映射。

## 3. 已写入配置

- 环境键：`zaozhuang:test`
- 兼容别名：`zaozhuang`
- 主机：`100.70.0.189`
- 端口：`22022`
- 远程目录：`/data/app/zzgjj-test/capinfo-gjj-jshs`
- 命名空间：`zzgjj-test`
- 财务核算包映射：`capinfo-gjj-busi-cwhs-jzgl-basic-svc-app.jar` -> `capinfo-gjj-busi-cwhs-jzgl-basic-svc-depl`
- 前端包映射：`capinfo-gjj-frontend-jshs-gm.zip` -> `capinfo-gjj-frontend-jshs-gm-depl`

## 4. 敏感信息处理

- `username` 使用占位符 `<SSH_USERNAME>`。
- `password` 使用占位符 `<SSH_PASSWORD>`。
- 未写入真实服务器账号、密码或私钥。

## 5. 校验结果

- 已使用 PowerShell `ConvertFrom-Json` 校验 JSON 格式，结果有效。
- Python 不可用，未使用 `python -m json.tool` 校验。
