# 20260508 prod主索引与区域大索引提示词更新记录

## 本次变更
- 更新 `doc/提示词/Gjj项目组前后端项目开发提示词.md`。
- 新增“项目索引提示词（prod 主索引 + 区域大索引）”章节。
- 统一索引策略为“先 `prod` 主索引，后区域差异索引”。
- 新增独立索引提示词文档 `doc/提示词/Gjj项目-prod主索引与区域大索引提示词.md`。
- 明确“区域”定义为与 `prod` 同级目录（如 `zaozhuang`、`linyi`、`wenzhou`、`jiaxing`）。

## 核心内容
- 固化后端聚合模块索引范围与分层目录映射。
- 增加 Controller/Service/Mapper 的方法级索引编制规则。
- 增加区域差异索引标准字段：差异类型、影响范围、回归要点。
- 增加索引输出模板，便于持续补录。
- 增加 `prod` 实际方法索引样本（`ZjJycshDmServiceImpl`、`YwglServiceImpl`、`YhtServiceImpl` 等）。

## 当前限制
- 区域差异目前以目录快照和关键文件命名为主，未对所有同名文件执行逐文件 diff。
