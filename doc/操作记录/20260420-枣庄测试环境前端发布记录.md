# 枣庄测试环境前端发布记录

## 发布目标

- 环境：`zaozhuang:test`
- 服务：`capinfo-gjj-frontend-jshs-gm-depl`
- 发布类型：前端单包发布

## 本次发布说明

本次按用户确认，发布的是**当前前端工作区整体改动**对应的构建产物，而不是仅限单个问题修复文件。

构建工程：

- `zaozhuang/WebstormProjects/capinfo-gjj-frontend-jshs-gm`

发布产物：

- `capinfo-gjj-frontend-jshs-gm.zip`

本次重点包含：

- `src/components/spzx/jzsp.vue` 中审批中心审核页滚动修复
- 当前前端工作区内其余一并参与构建的改动

## 执行过程

### 1. 工具链切换

- 使用 `vfox use nodejs@16.20.2`

### 2. 前端构建

执行流程：

```bash
rm -rf dist
npm install
npm run build
zip -rq capinfo-gjj-frontend-jshs-gm.zip dist
```

### 3. 发布产物整理

将产物复制到：

- `zaozhuang/release/capinfo-gjj-frontend-jshs-gm.zip`

### 4. 上传到枣庄测试环境

执行命令：

```bash
python3 scripts/remote_deploy.py zaozhuang:test capinfo-gjj-frontend-jshs-gm.zip
```

上传结果：

- 上传成功

### 5. 重启前端服务

执行命令：

```bash
python3 scripts/remote_exec.py zaozhuang:test "kubectl rollout restart deployment capinfo-gjj-frontend-jshs-gm-depl -n {namespace}"
```

重启结果：

- `deployment.apps/capinfo-gjj-frontend-jshs-gm-depl restarted`

### 6. Rollout 验证

执行命令：

```bash
python3 scripts/remote_exec.py zaozhuang:test "kubectl rollout status deployment capinfo-gjj-frontend-jshs-gm-depl -n {namespace} --timeout=180s"
```

验证结果：

- `deployment "capinfo-gjj-frontend-jshs-gm-depl" successfully rolled out`

## 结论

本次前端包已成功发布到枣庄测试环境，且前端部署已成功滚动完成。

建议后续验证项：

1. 审批中心打开字段较多的审核任务
2. 验证 `申请数据` 页签是否已出现纵向滚动条
3. 验证普通审批详情页无明显布局回归

