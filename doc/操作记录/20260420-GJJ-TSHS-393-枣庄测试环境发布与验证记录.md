# GJJ-TSHS-393 枣庄测试环境发布与验证记录

## 问题信息

- 编号：`GJJ-TSHS-393`
- 标题：`结算核算-账户管理-业务收付账户对照，样式问题，银行专户信息和银行名称较多时，会显示到边框外`

## 本次修复

修复文件：

- `zaozhuang/WebstormProjects/capinfo-gjj-frontend-jshs-gm/src/components/cwhs/sfyhdz.vue`

修复内容：

1. 给“收付银行对照银行专户顺序号修改”弹窗中的两列文本增加包裹类：
   - `银行专户信息`
   - `银行名称`
2. 新增 `.cell-wrap` 样式，使长文本在单元格内自动换行，不再顶出边框

关键样式：

```css
.cell-wrap {
    display: block;
    width: 100%;
    white-space: normal;
    word-break: break-all;
    overflow-wrap: anywhere;
    line-height: 20px;
}
```

## 打包与发布

### 工具链

- `vfox use nodejs@16.20.2`

### 构建

执行了前端重新构建与打包：

```bash
rm -rf dist
npm install
npm run build
zip -rq capinfo-gjj-frontend-jshs-gm.zip dist
```

### 发布目标

- 环境：`zaozhuang:test`
- 服务：`capinfo-gjj-frontend-jshs-gm-depl`

### 发布命令

```bash
python3 scripts/remote_deploy.py zaozhuang:test capinfo-gjj-frontend-jshs-gm.zip
python3 scripts/remote_exec.py zaozhuang:test "kubectl rollout restart deployment capinfo-gjj-frontend-jshs-gm-depl -n {namespace}"
python3 scripts/remote_exec.py zaozhuang:test "kubectl rollout status deployment capinfo-gjj-frontend-jshs-gm-depl -n {namespace} --timeout=180s"
```

### 发布结果

- 文件上传成功
- 前端 deployment 重启成功
- rollout 状态成功

## 系统验证

### 验证路径

1. 登录枣庄测试系统
2. 进入 `结算核算子系统`
3. 进入 `资金结算 -> 账户管理 -> 业务收付账户对照`

### 验证结果

当前测试环境该页面查询结果为 `0 条`，因此**无法直接在真实长文本数据上做最终肉眼验证**。

但已确认以下关键事实：

1. 线上页面已打开到正确业务页
2. 运行时样式表中已能读取到本次新增规则：

```css
.cell-wrap[data-v-ff738710] {
    display: block;
    width: 100%;
    white-space: normal;
    word-break: break-all;
    overflow-wrap: anywhere;
    line-height: 20px;
}
```

这说明：

- 本次修复代码已经随前端发布包成功下发到枣庄测试环境
- 当前缺少可复现的长文本业务数据，暂时无法完成最终视觉验收

## 后续建议

建议在测试环境补一条“银行专户信息 / 银行名称较长”的收付银行对照数据后，继续验证：

1. 打开“收付银行对照银行专户顺序号修改”弹窗
2. 检查两列长文本是否在单元格内换行
3. 确认不再越过表格边框

