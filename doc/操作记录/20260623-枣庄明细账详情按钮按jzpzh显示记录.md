# 20260623-枣庄明细账详情按钮按jzpzh显示记录

## 1. 需求说明

- 页面：`zaozhuang/WebstormProjects/capinfo-gjj-frontend-jshs-gm/src/components/cwhs/zbgl/newmxz.vue`
- 需求：明细账列表中的“详情”按钮仅在 `jzpzh` 不为空时显示。

## 2. 本次调整

- 在明细账列表 `rowbar.buttons` 的“详情”按钮配置中新增：

```js
show: (row) => !!(row && row.jzpzh)
```

## 3. 调整效果

- 当行数据存在 `jzpzh` 时，显示“详情”按钮。
- 当 `jzpzh` 为空、`null` 或不存在时，不显示“详情”按钮。

## 4. 验证情况

- 已执行静态诊断，未发现本次改动引入的新错误。
- 已复核差异，确认改动范围仅限按钮显示条件。
