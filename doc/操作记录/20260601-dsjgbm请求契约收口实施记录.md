# 20260601-dsjgbm请求契约收口实施记录

## 1. 本轮目标

继续沿“一户通内部参数不暴露给业务端”的方向，把收付款对外请求契约中的 `dsjgbm` 删除。

## 2. 实施范围

本轮仅处理以下 6 个对外收付款请求 DTO：

1. `SkywDbskAddReqDTO`
2. `SkywDbskZdzhAddReqDTO`
3. `DbskAddV2ReqDTO`
4. `DbfkAddV2ReqDTO`
5. `PlskAddV2ReqDTO`
6. `PlfkAddV2ReqDTO`

## 3. 测试先行

在 `YhtYwServiceImplTest` 中新增 6 条契约级断言，使用反射校验 DTO 不再暴露 `dsjgbm`：

1. `skywDbskAddReqDtoShouldNotExposeDsjgbmField`
2. `skywDbskZdzhAddReqDtoShouldNotExposeDsjgbmField`
3. `dbskAddV2ReqDtoShouldNotExposeDsjgbmField`
4. `dbfkAddV2ReqDtoShouldNotExposeDsjgbmField`
5. `plskAddV2ReqDtoShouldNotExposeDsjgbmField`
6. `plfkAddV2ReqDtoShouldNotExposeDsjgbmField`

## 4. 实际改动

已从上述 6 个 DTO 中删除：

1. `dsjgbm`

本轮不改动：

1. 一户通内部签约 DTO
2. 内部实体
3. 费项映射实现
4. 状态同步链路

## 5. 调用链复核

复查后，当前残留的 `getDsjgbm()` 主要位于：

1. `YhtController`
2. `YhtSceneParamResolver`
3. `YhtServiceImpl`
4. `YhtProtocolRequestValidator`
5. `YhtProtocolRequestOrchestrator`
6. `WbjkServiceImpl` 的签约/解约相关请求对象

这些位置不属于本轮收口的 6 个收付款外层请求 DTO。

## 6. 诊断与验证

本轮已执行：

1. `GetDiagnostics`
   - DTO 文件与测试文件未见新增语法错误
   - 当前 IDE 仍将相关文件标记为 `non-project file`
2. `scripts/guardrails/agent-delivery-guardrail.sh --scan-only`
   - 退出码 `0`
   - 输出为“未发现需要扫描的目标文件”
3. `mvn -Dtest=YhtYwServiceImplTest -Dsurefire.useFile=false test`
   - 已将输出落盘到 `/tmp/yht_dsjgbm_contract_test.log`
   - 实际结果为 `BUILD FAILURE`
   - 失败原因仍是依赖解析被 `maven-default-http-blocker` 拦截
   - 关键缺失依赖包括：
     - `com.tienon:refbdcutil:jar:1.0`
     - `cn.capinfo.framework:cipher:jar:1.0`
4. 既有告警仍存在：
   - `PlskAddV2ReqDTO` 的 `@EqualsAndHashCode` 未显式声明 `callSuper`

因此本轮只能确认：

1. 契约代码已完成收口
2. 诊断未见新增语法错误
3. 测试已尝试执行，但未能跑到断言层

## 7. 本轮结论

本轮已把 `dsjgbm` 从 6 个收付款对外请求 DTO 中删除，进一步明确：

1. 收付款外层契约不再暴露 `dsjgbm`
2. `dsjgbm` 只保留在一户通内部真正需要的链路中
