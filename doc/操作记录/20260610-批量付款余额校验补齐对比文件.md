## 背景

- 用户初始反馈：批量付款请求未校验付款账户余额是否充足。
- 本次返工反馈：上一次修改未完整落入当前文件，实际只补了批量余额判断，未完成提示文案调整，也未把中心账户判断移出付款限制参数块。

## 目标文件

- `prod/IdeaProjects/capinfo-gjj-busi-jshs/capinfo-gjj-busi-zjjs-ywgl/capinfo-gjj-busi-zjjs-ywgl-basic-svc-busi/src/main/java/cn/capinfo/gjj/busi/zjjs/ywgl/busi/service/impl/YwglServiceImpl.java`

## 计划修改点

### 1. 单笔付款余额提示

- 方法：`ckeckFkyw(Fkyw fkyw)`
- 调整：将余额不足提示从 `交易账户余额不足` 改为 `中心账户：{yhzhhm}，余额不足`。

```java
if (fkyw.getJe().compareTo(yhzhYexx.getJzye()) > 0) {
    fkyw.setJblx(CommonCodeConstants.JSJBLX_M);
    fkyw.setSpzt(CommonCodeConstants.SPZT.ROUND1.DSP);
    fkyw.setJsxyxx("中心账户：" + fkyw.getYhzhhm() + "，余额不足");
    log.info("{}", fkyw.getJsxyxx());
    return fkyw;
}
```

### 2. 批量付款请求校验入口

- 方法：`checkFkywPlfk(FkywPlfkAddReqDTO fkywPlfkAddReqDTO)`
- 调整：
  - 在付款限制参数校验前执行 `validateBatchCounterpartyAccounts(...)`
  - 即使未配置付款限制参数，也会校验“收付方账号不能一样 / 对手账号不能是中心账号”
  - 余额不足提示同步改为带中心账号格式

### 3. 批量付款手工/复用校验入口

- 方法：`checkFkywPlfk(Fkyw fkyw, List<FkywPlmx> fkywPlmxList)`
- 调整：
  - 同步在付款限制参数校验前执行 `validateBatchCounterpartyAccounts(...)`
  - 删除付款限制参数块内重复的中心账户判断
  - 余额不足提示同步改为带中心账号格式

### 4. 复用辅助方法

- 新增：`buildCenterAccountInsufficientMessage(String yhzhhm)`
- 新增：`validateBatchCounterpartyAccounts(String payerAccount, List<String> payeeAccounts)`

## 预期结果

- 单笔与批量付款在余额不足时统一返回 `中心账户：{yhzhhm}，余额不足`
- 批量付款即使未设置付款限制参数，也会继续校验中心账户相关规则
- 保持现有留存余额、单笔限额、当日计划额度等规则不变
