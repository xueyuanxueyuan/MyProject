# 枣庄住建部联调环境打包与部署实施记录

**文档编号**：GJJ-DEPLOY-20260509-01  
**实施时间**：2026-05-09  
**操作人**：AI 架构实施组 (兵部 & 礼部)  

---

## 1. 部署背景
在完成了自动发交易任务双维调度（`execode` 通道 + `ywlx`/`xtlx`/`yhdm` 复合业务分组）重构，并在交易初始化表中补全了缺失的字段及防呆校验逻辑后，用户指令要求将最新代码打包并发布至**枣庄住建部联调环境**（`test-zjb`）以供联调测试。

## 2. 实施步骤

### 2.1 编译打包 (Build)
1. **工具链校验**：使用 `vfox` 切换至项目要求的 `java@17.0.2+8` 和 `maven@3.9.14`，确保编译环境完全一致。
2. **代码编译**：在 `zaozhuang/IdeaProjects/capinfo-gjj-busi-jshs` 目录下执行了完整的 Maven 编译打包命令：
   ```bash
   mvn clean package -DskipTests -s settings-tsp.xml
   ```
   编译顺利通过（BUILD SUCCESS），生成了包含最新 `ywgl` 核心模块在内的各微服务 JAR 包。
3. **发布包聚合**：将编译生成的 `*-app.jar` 统一复制聚合至 `zaozhuang/release/` 目录中，准备进行远程传输。

### 2.2 远程发布 (Deploy)
1. **目标环境确认**：根据 `config/deploy_servers.json` 配置，确认部署目标为 `zaozhuang:test-zjb`，对应 Namespace 为 `zzgjj-test-zjb`，目标服务器 IP 为 `100.70.0.189`。
2. **包体上传**：调用项目内置的 `remote_deploy.py` 脚本，将 `zaozhuang/release/` 目录下的 7 个微服务执行包（含 `capinfo-gjj-busi-zjjs-ywgl-basic-svc-app.jar` 等）成功传输至服务器目标路径。
3. **滚动重启**：调用 `remote_exec.py` 脚本，在远程服务器上执行了 `kubectl rollout restart deployment` 命令，对受影响的 7 个 Deployment 进行了平滑滚动重启。
   ```bash
   kubectl rollout restart deployment capinfo-gjj-busi-zjjs-sp-basic-svc-depl capinfo-gjj-busi-cwhs-jzgl-basic-svc-depl capinfo-gjj-busi-zjjs-zhgl-basic-svc-depl capinfo-gjj-busi-jhgl-zjjh-basic-svc-depl capinfo-gjj-busi-zjjs-ywgl-basic-svc-depl capinfo-gjj-busi-jshs-gm-agg-svc-depl capinfo-gjj-busi-zjjs-lcgl-basic-svc-depl -n zzgjj-test-zjb
   ```

## 3. 部署结论
- **代码版本**：已包含最新的 `TradeDispatchCoordinator` 双维调度重构逻辑及 `ZjbDbywJobHandler` 单笔入口改造。
- **运行状态**：枣庄联调环境的 K8s 容器集群已成功触发滚动更新。
- **后续动作**：待容器启动完毕后，联调团队可直接触发定时任务，观察日志中是否输出 `----------启动双维调度：获取通道内所有待发送的三维复合分组 (ywlx, xtlx, yhdm)--------------` 标志性内容。