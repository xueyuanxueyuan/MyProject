# 20260512 提交规则模板调整与 codeworkfiles 规则清理记录

## 1. 需求
- 用户要求调整提交格式为以下顺序：
  - `feat: 你的提交标题（AI生成提交）`
  - `AI辅助：true`
  - `AI工具：Trae`
  - `1. 改了什么`
  - `2. 为了解决什么问题`
- 用户要求生成可复用模板，并放到 `doc` 目录下的相应规范目录。
- 用户要求废弃 `codeworkfiles/` 目录，不再在该目录生成留痕文件，也不再与该目录做对比；相关规范需删除。

## 2. 处理思路
- 先检索仓库内现有提交规范入口，确认需要同步修改的主规范文件。
- 在 `doc/项目规范/` 下新增独立模板文档，作为后续直接复用入口。
- 删除规范中要求写入 `codeworkfiles/` 的条款，并同步修正文档中对该目录的流程性引用。
- 删除本次上轮任务生成的 `codeworkfiles` 对比留痕文件，避免新旧规则冲突。

## 3. 修改文件
- `doc/项目规范/Gjj项目组智能体协同开发规范.md`
- `doc/项目规范/项目开发规范.md`
- `doc/项目规范/Git提交描述模板.md`
- `doc/架构设计/edict-trae-architecture.md`
- `doc/操作记录/20260512-提交规则增加AI辅助与AI工具记录.md`

## 4. 规则调整结果
- 保留原有 `（AI生成提交）` 要求。
- 明确标题格式默认为 `feat: 你的提交标题（AI生成提交）`。
- 明确提交描述顺序必须为：
  - `AI辅助：true`
  - `AI工具：Trae`
  - `1. 改了什么`
  - `2. 为了解决什么问题`
- 删除了项目规范中“必须将步骤说明和留痕写入 `codeworkfiles/`”的规则。
- 删除了项目规范中关于 `codeworkfiles/` 作为留痕边界目录的规则。
- 将架构文档中的流程性描述改为“在 `doc` 目录对应分类下生成记录”。

## 5. 风险与说明
- 本次仅调整规范文档与模板文档，未改动 Git Hook、提交脚本或自动生成器。
- 根目录 `README.md` 已存在未提交改动，本次为避免覆盖用户现有工作，未同步改写该文件示例。
- 历史文档中仍可能存在把 `codeworkfiles/` 作为上游源码目录或历史数据目录的描述；本次仅清理“规范性、流程性、留痕性”规则，不主动改写全部历史背景记录。

## 6. 验证
- 通过全文检索确认新提交格式已写入两处项目规范文件，并已新增独立模板文档。
- 通过全文检索确认项目规范与架构文档中不再保留 `codeworkfiles/` 的流程性留痕规则。
- 通过守卫脚本执行扫描验证，确认本次新增文档与规则文本无占位交付问题。
