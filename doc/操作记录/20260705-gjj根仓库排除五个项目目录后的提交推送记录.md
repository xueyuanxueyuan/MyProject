# Gjj 根仓库排除五个项目目录后的提交推送记录

## 1. 操作背景

用户要求将 `D:\Probject\Gjj` 根仓库中，除以下五个具体开发项目目录之外的其他文件统一提交并推送到个人远程仓库：

- `D:\Probject\Gjj\prod`
- `D:\Probject\Gjj\wenzhou`
- `D:\Probject\Gjj\zaozhuang`
- `D:\Probject\Gjj\jiaxing`
- `D:\Probject\Gjj\linyi`

个人远程仓库地址为：

- `https://github.com/xueyuanxueyuan/MyProject.git`

## 2. 范围确认

本次先核对根仓库状态，确认上述五个目录当前没有待提交变更；现有待处理内容位于根目录及 `doc` 相关路径，主要包括：

- 根目录 `README.md`、`.gitignore`
- `doc/技能库` 下新增技能资料
- `doc/操作记录` 下新增记录文件
- `doc/提示词` 等文档目录中的已有修改

因此，本次提交范围定义为：

- 包含：除五个排除目录之外的全部已修改、已新增文件
- 排除：`prod`、`wenzhou`、`zaozhuang`、`jiaxing`、`linyi`

## 3. 执行策略

由于根仓库本地 `main` 与个人远程仓库 `origin/main` 历史并不一致，直接推送存在被拒绝风险，因此采用以下安全策略：

1. 先基于远程 `origin/main` 获取基线
2. 在不纳入五个排除目录的前提下，整理当前根仓库的其余变更
3. 生成符合《第一研发中心-代码Commit提交规范说明》的提交
4. 推送到个人远程仓库 `main`

## 4. 提交规范

本次提交消息需遵循如下格式：

```text
docs(repo): 同步根仓库公共资料

AI辅助: true
AI工具: Trae
```

## 5. 结果说明

本文件用于留痕本次“排除五个业务项目目录之外的根仓库资料统一提交推送”操作，最终以实际 Git 提交号、推送结果和工作区核验结果为准。
