# 嘉兴邮储、交行、浙商当日流水下载与账户客户有效性校验测试记录

## 任务说明

- 测试对象：
  - 邮储银行 `313009`
  - 交通银行 `301000`
  - 浙商银行 `313041`
- 测试环境：`http://k8s.jxgjj:31129`
- 测试接口：
  - `test_main.py::TestOther::test_zhdrlscx`
  - `test_main.py::TestOther::test_khzhyxx_cx`

## 入口核实

- 统一选择器 `run_main.py --select "当日交易流水和账户校验"` 虽然能正确筛出接口，但本轮实际执行时没有切到三家银行各自的原始 Excel，而是统一落到了 `中行测试数据_配置版.xlsx`，请求中的 `yhdm` 也变成了 `104000`。
- 因此该入口本轮不作为三家银行真实结果依据。
- 最终采用显式指定 `TEST_DATA_FILE` 的方式，分别对三家银行原始 Excel 直接执行真实测试。

## 执行命令

### 1. 交通银行

```bash
cd /home/source/Jetbrains/Probject/Gjj/gjj-automation-test
REGION_CODE=jx BASE_URL=http://k8s.jxgjj:31129 \
TEST_DATA_FILE='test_data/jx/银行测试数据收集表_交通银行.xlsx' \
SESSION_REPORT_BATCH_ID=jx_bcm_other_20260522 \
python3 -m pytest test_main.py::TestOther::test_zhdrlscx test_main.py::TestOther::test_khzhyxx_cx -v --tb=short -s
```

### 2. 邮储银行

```bash
cd /home/source/Jetbrains/Probject/Gjj/gjj-automation-test
REGION_CODE=jx BASE_URL=http://k8s.jxgjj:31129 \
TEST_DATA_FILE='test_data/jx/银行测试数据收集表_邮储.xlsx' \
SESSION_REPORT_BATCH_ID=jx_psbc_other_20260522 \
python3 -m pytest test_main.py::TestOther::test_zhdrlscx test_main.py::TestOther::test_khzhyxx_cx -v --tb=short -s
```

### 3. 浙商银行

```bash
cd /home/source/Jetbrains/Probject/Gjj/gjj-automation-test
REGION_CODE=jx BASE_URL=http://k8s.jxgjj:31129 \
TEST_DATA_FILE='test_data/jx/嘉兴公积金银行测试数据收集表（浙商银行）.xlsx' \
SESSION_REPORT_BATCH_ID=jx_zs_other_20260522 \
python3 -m pytest test_main.py::TestOther::test_zhdrlscx test_main.py::TestOther::test_khzhyxx_cx -v --tb=short -s
```

## 测试结果

### 交通银行

- 当日流水下载：成功，返回 `code=0`
- 账户客户有效性校验：成功，返回 `code=0`
- 报告：
  - `gjj-automation-test/reports/pytest_multisession_report_jx_bcm_other_20260522.md`
  - `gjj-automation-test/reports/pytest_multisession_report_jx_bcm_other_20260522.html`

### 邮储银行

- 当日流水下载：成功，返回 `code=0`
- 账户客户有效性校验：接口响应 `code=0`，但业务返回提示 `证件号码不符`
- 报告：
  - `gjj-automation-test/reports/pytest_multisession_report_jx_psbc_other_20260522.md`
  - `gjj-automation-test/reports/pytest_multisession_report_jx_psbc_other_20260522.html`

### 浙商银行

- 当日流水下载：成功，返回 `code=0`
- 账户客户有效性校验：成功，返回 `code=0`
- 报告：
  - `gjj-automation-test/reports/pytest_multisession_report_jx_zs_other_20260522.md`
  - `gjj-automation-test/reports/pytest_multisession_report_jx_zs_other_20260522.html`

## 结论

- 嘉兴交通银行、浙商银行在本轮的“当日流水下载”和“账户客户有效性校验”均成功。
- 嘉兴邮储银行“当日流水下载”成功，但“账户客户有效性校验”返回业务提示 `证件号码不符`，因此不能视为该项校验通过。
- 本轮汇总结论基于三家银行原始 Excel 的真实执行结果，而不是统一选择器误落中行配置版后的伪结果。
