# 20260613-枣庄试算平衡前端聚合层实现记录

## 1. 本次目标

- 在后端试算平衡接口基础上，继续补充：
  - 后端关键注释
  - 前端页面
  - 前端 API
  - gm-agg 聚合层转发

## 2. 已完成内容

### 2.1 注释补充

- 为 `SsphCategorySupport` 补充了一级科目分类和公式注释
- 为 `SsphZbglMapper.xml` 补充了取数口径注释
- 为 `CwhsZbglServiceImpl.querySsphZbgl` 补充了服务层计算说明

### 2.2 前端实现

- 新增页面：`src/components/cwhs/zbgl/ssph.vue`
- 查询条件：
  - 账套
  - 截至日期
- 查询结果：
  - 资产
  - 负债
  - 净资产
  - 收入
  - 支出
  - 资产合计
  - 负债合计
  - 差额
  - 平衡结果

### 2.3 前端 API

- 在 `src/api/cwhs/zbgl.js` 中新增 `querySsphZbgl`

### 2.4 聚合层实现

- 在 `gm-agg` 的 `CwhsZbglController` 中新增 `/api/v1/cwhs/querySsphZbgl`
- 在 `gm-agg` 的 `CwhsZbglService / CwhsZbglServiceImpl` 中新增 `querySsphZbgl`

## 3. 验证情况

- 前端页面 `ssph.vue` 诊断通过
- 前端 API `zbgl.js` 诊断通过
- 聚合层 `CwhsZbglController / CwhsZbglServiceImpl` 诊断通过
- `git diff --check` 通过
- 交付门禁扫描通过

## 4. 当前限制

- 前端菜单由后端动态资源下发，不在当前前端仓库静态路由中配置
- 因此页面文件虽已落地，但若要出现在正式菜单中，还需补充资源菜单配置
