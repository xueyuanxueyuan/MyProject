# 嘉兴邮储全量接口测试记录

## 任务说明

- 目标：测试嘉兴邮储全量接口
- 执行方式：按当前自动化定义的全量非签约主测试执行 `test_main.py`
- 测试数据文件：`gjj-automation-test/test_data/jx/银行测试数据收集表_邮储.xlsx`
- 测试环境：`http://k8s.jxgjj:31129`
- 批次标识：`jx_psbc_full_20260522`

## 执行命令

```bash
cd /home/source/Jetbrains/Probject/Gjj/gjj-automation-test
REGION_CODE=jx BASE_URL=http://k8s.jxgjj:31129 \
TEST_DATA_FILE='test_data/jx/银行测试数据收集表_邮储.xlsx' \
SESSION_REPORT_BATCH_ID=jx_psbc_full_20260522 \
python3 -m pytest test_main.py -v --tb=short -s
```

## 执行结果

- 退出码：`1`
- 总计：`13` 项
- 结果：`12 passed, 1 failed, 1 warning`
- 耗时：`3.59s`

## 通过情况摘要

- 交易类：
  - 单笔收款：业务成功，返回 `code=0`
  - 单笔付款：业务成功，返回 `code=0`
  - 批量收款：业务返回 `code=10000`，提示 `存在重复明细流水号，请核实！`
  - 批量扣款：业务返回 `code=10000`，提示 `存在重复明细流水号，请核实！`
  - 批量付款：业务返回 `code=10000`，提示 `存在重复明细流水号，请核实！`
- 查询类：
  - 客户账户有效查询：HTTP 成功，但业务提示 `证件号码不符`
  - `BDC112Send`：成功，返回 `code=0`
  - 银行当日流水下载：成功，返回 `code=0`

## 失败项

- 失败用例：`test_main.py::TestOther::test_download_zh_jymx[test_case0]`
- 接口：`/api/v1/ywgl/downloadZhJymx`
- 实际响应：HTTP `500`
- 断言失败：期望状态码 `200`，实际为 `500`
- 服务端异常摘要：
  - `/api/v1/zhgl/getOneYhzhxx` 下游接口异常
  - `Expected one result (or null) to be returned by selectOne(), but found: 16`

## 结果说明

- 本轮“嘉兴邮储全量接口”不能按全量通过汇报。
- 失败主因是“账户交易明细下载”接口命中后端数据异常，导致整轮全量测试存在明确失败项。
- 同时，批量交易类虽未在 `pytest` 里判失败，但业务层面仍持续报 `mxlsh` 重复；客户账户有效查询也有业务提示，说明全量结果需按真实业务返回理解，而不能仅看 `pytest passed` 数量。

## 输出报告

- JSON：`gjj-automation-test/reports/session_report_batches/jx_psbc_full_20260522.json`
- Markdown：`gjj-automation-test/reports/pytest_multisession_report_jx_psbc_full_20260522.md`
- HTML：`gjj-automation-test/reports/pytest_multisession_report_jx_psbc_full_20260522.html`
- 最新报告入口：`gjj-automation-test/reports/latest_session_report.html`
