# 20260623-枣庄前端测试环境单包发布记录-总账跳明细账按日查询修正

## 1. 发布目标

- 地区：`zaozhuang`
- 环境：`test`
- 发布方式：单包发布
- 发布范围：仅前端
- 发布包名：`capinfo-gjj-frontend-jshs-gm.zip`
- 目标命名空间：`zzgjj-test`
- 目标部署：`capinfo-gjj-frontend-jshs-gm-depl`

## 2. 本次发布对应改动

- 修正总账页面点击详情跳转明细账时的日期承接逻辑。
- 从总账跳转明细账时，按当前行对应的具体日期查询，不再查询整月明细数据。
- 相关改动文件：
  - `zaozhuang/WebstormProjects/capinfo-gjj-frontend-jshs-gm/src/components/cwhs/zbgl/newmxz.vue`

## 3. 代码来源

- 按当前本地工作区代码直接打包。
- 未执行 `git pull`。

## 4. 工具链校验

执行命令：

```bash
source ~/.bashrc
vfox use nodejs@16.20.2
eval "$(vfox env -s bash)"
node -v
npm -v
```

校验结果：

- Node.js：`v16.20.2`
- npm：`8.19.4`

## 5. 打包命令

在目录 `zaozhuang/WebstormProjects/capinfo-gjj-frontend-jshs-gm` 执行：

```bash
rm -rf dist
npm install
npm run build
zip -r capinfo-gjj-frontend-jshs-gm.zip dist
find /home/source/Jetbrains/Probject/Gjj/zaozhuang/release -maxdepth 1 -type f -name "capinfo-gjj-frontend-jshs-gm.zip" -delete
cp capinfo-gjj-frontend-jshs-gm.zip /home/source/Jetbrains/Probject/Gjj/zaozhuang/release/
```

结果：

- 前端构建成功。
- 发布压缩包已归档到：`zaozhuang/release/capinfo-gjj-frontend-jshs-gm.zip`

## 6. 构建告警说明

本次构建成功，但存在非阻断告警：

- `getOrgList` 导出缺失告警
- `butterfly-dag` 的 SVG 解析告警
- 静态资源与入口体积过大告警
- `caniuse-lite` 版本过旧提示

上述告警未阻断本次发布。

## 7. 归档包校验

执行命令：

```bash
sha256sum /home/source/Jetbrains/Probject/Gjj/zaozhuang/release/capinfo-gjj-frontend-jshs-gm.zip
```

校验值：

```text
d5ae0cfc80216377c54e22e28f29265b26a99cf25075c3f568341df48c3a346c
```

## 8. 远程上传

执行命令：

```bash
python3 /home/source/Jetbrains/Probject/Gjj/scripts/remote_deploy.py \
  zaozhuang:test \
  capinfo-gjj-frontend-jshs-gm.zip
```

执行结果：

- 已连接 `100.70.0.189:22022`
- 单包上传成功
- 远端目标路径：`/data/app/zzgjj-test/capinfo-gjj-jshs`

## 9. 服务重启

执行命令：

```bash
python3 /home/source/Jetbrains/Probject/Gjj/scripts/remote_exec.py \
  zaozhuang:test \
  "kubectl rollout restart deployment capinfo-gjj-frontend-jshs-gm-depl -n {namespace} && kubectl rollout status deployment capinfo-gjj-frontend-jshs-gm-depl -n {namespace} --timeout=240s"
```

执行结果：

- `deployment.apps/capinfo-gjj-frontend-jshs-gm-depl restarted`
- `deployment "capinfo-gjj-frontend-jshs-gm-depl" successfully rolled out`

## 10. 结论

- 枣庄测试环境前端单包已完成打包、上传和滚动重启。
- 当前可在测试环境重点验证总账点击详情后，明细账是否只展示当前日期数据。
