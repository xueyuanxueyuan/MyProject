# 20260509-枣庄-凭证汇总改造-V1.1 实施操作记录（AI 生成）

## 1. 任务背景

依据 [详细设计 V1.1](../设计文档/20260509-枣庄项目-凭证新增中心账号与业务分类及汇总口径调整-详细设计-V1.1.md) 与 [评审通过报告](20260509-枣庄项目-凭证汇总改造设计最终评审报告.md)，在 `capinfo-gjj-busi-cwhs-jzgl` 模块实现：
- 凭证主表 `cwhs_pz` 与汇总凭证主表 `cwhs_hzpz` 新增 `yhzhhm`（中心账号）、`ywfl`（业务分类）。
- 凭证生成主链路 `ywjzPz` 注入两字段并落库；`ywfl` 由 `ParamUtil.getCode` 按 `ywlx` 映射至 `codeZl1`，缺失按开关回退。
- 汇总查询 `JzpzDOMapper#getJzpzhz` 改为按 `yhzhhm + ywfl` 聚合，保留 `getJzpzhzLegacy` 用于运行时回退。
- 实装 `voucher.summary.byYwfl`、`voucher.mapping.strictMode`、`voucher.mapping.fallback.enable` 三档配置开关。

## 2. 与用户已确认的实施口径

| 决策项 | 选择 |
| --- | --- |
| `yhzhhm` 兜底 | 仅取 `ywlszbDOList.get(0).getYhzhhm()`，缺失归并"其他账号" |
| 配置开关 | 实装 `application.yml` + `@Value` 注入三档开关 |
| DDL 脚本范围 | 仅 zaozhuang 区域脚本（不出 prod 主索引版） |
| `addHzpzBatch` 携带方式 | 入参 `Hzpz` 携带 `yhzhhm/ywfl`，SQL 直接 `#{hzpz.xxx}` |

## 3. 变更文件清单

### 3.1 SQL（运维侧执行）
- `doc/数据库脚本/20260509-枣庄-凭证汇总改造-V1.1.sql`（DDL + 索引）
- `doc/数据库脚本/20260509-枣庄-凭证汇总改造-V1.1-rollback.sql`（回滚）

### 3.2 模块内代码（`zaozhuang/IdeaProjects/capinfo-gjj-busi-jshs/capinfo-gjj-busi-cwhs-jzgl`）

| 模块 | 路径 | 改动摘要 |
| --- | --- | --- |
| api | `.../api/dto/req/PzZdReqDTO.java` | 追加字段 `yhzhhm/ywfl` |
| api | `.../api/dto/resp/JzpzhzRespDTO.java` | 追加字段 `yhzhhm/ywfl` |
| busi | `.../domain/dao/PzDO.java` | 追加 `@TableField` 字段 `yhzhhm/ywfl` |
| busi | `.../domain/dao/HzpzDO.java` | 追加 `@TableField` 字段 `yhzhhm/ywfl` |
| busi | `.../domain/entity/Pz.java` | 追加字段 `yhzhhm/ywfl` |
| busi | `.../domain/entity/Hzpz.java` | 追加字段 `yhzhhm/ywfl` |
| busi | `.../domain/dao/mapper/PzDOMapper.xml` | `BaseResultMap` + `Base_Column_List` 追加列 |
| busi | `.../domain/dao/mapper/HzpzDOMapper.xml` | `BaseResultMap`、`Base_Column_List`、`insertHzpzBatch`（INSERT+SELECT 列）、`addHzpzBatch`（INSERT 列与入参） 同步 |
| busi | `.../domain/dao/mapper/JzpzDOMapper.java` | 新增方法 `getJzpzhzLegacy(PzhzReqDTO)` |
| busi | `.../domain/dao/mapper/JzpzDOMapper.xml` | `getJzpzhz` 改为按 `yhzhhm + ywfl` 聚合（保留 `xtlx/ywlx/jzlxfl` 向后兼容）；新增 `getJzpzhzLegacy` 保留旧版 |
| busi | `.../service/impl/CwhsServiceImpl.java` | ① 注入 3 个 `@Value` 开关；② 新增 `mapYwfl/mapYwflName` 私有方法；③ `ywjzPz` 在保存前注入 `yhzhhm/ywfl`；④ `zdpzhz` 汇总落 `yhzhhm/ywfl` 并改用 `mapYwflName` 作摘要；⑤ `pzhz` 第一个调用点按 `summaryByYwfl` 切换聚合 SQL |
| app | `.../src/main/resources/application.yml` | 追加 `voucher.summary.byYwfl` / `voucher.mapping.strictMode` / `voucher.mapping.fallback.enable` |

## 4. 关键代码片段

### 4.1 `CwhsServiceImpl#ywjzPz` 注入（保存凭证前）
```java
pzZdReqDTO.setJbjgbh(ywlszbDOList.get(0).getJbjgbh());
pzZdReqDTO.setJbjgmc(ywlszbDOList.get(0).getJbjgmc());
// V1.1: 注入中心账号(yhzhhm)与业务分类(ywfl)
String yhzhhm = ywlszbDOList.get(0).getYhzhhm();
pzZdReqDTO.setYhzhhm(StrUtil.isNotBlank(yhzhhm) ? yhzhhm : "其他账号");
pzZdReqDTO.setYwfl(mapYwfl(ywjzReqDTO.getXtlx(), ywjzReqDTO.getYwlx()));
log.info("ywjzPz 注入维度 yhzhhm={}, ywfl={}, ywlx={}",
        pzZdReqDTO.getYhzhhm(), pzZdReqDTO.getYwfl(), ywjzReqDTO.getYwlx());
```

### 4.2 字典映射工具
```java
private String mapYwfl(String xtlx, String ywlx) {
    R<CsSjzdDTO> r = ParamUtil.getCode(Constants.HSDWDM, xtlx, "ywlx", ywlx);
    if (r != null && r.getIsSuccess() && r.getData() != null
            && StrUtil.isNotBlank(r.getData().getCodeZl1())) {
        return r.getData().getCodeZl1();
    }
    if (mappingStrictMode) {
        throw new BizException(ResultErrorCodeEnum.FAIL.getCode(),
                "业务分类映射缺失: ywlx=" + ywlx + ", xtlx=" + xtlx);
    }
    log.warn("ywfl 映射回退 ywlx={}, xtlx={}, fallback={}", ywlx, xtlx, mappingFallbackEnable);
    return mappingFallbackEnable && StrUtil.isNotBlank(ywlx) ? ywlx : "其他分类";
}
```

### 4.3 汇总查询切换（`pzhz` 调用 line 2044 附近）
```java
List<JzpzhzRespDTO> jzpzhzaRespDTOList = summaryByYwfl
        ? jzpzDOMapper.getJzpzhz(pzhzReqDTO)
        : jzpzDOMapper.getJzpzhzLegacy(pzhzReqDTO);
```

### 4.4 `getJzpzhz` 改造（核心）
```xml
SELECT
    a.jzlxfl, a.xtlx, a.ywlx,
    NVL(a.yhzhhm, '其他账号') AS yhzhhm,
    NVL(a.ywfl,   '其他分类') AS ywfl,
    b.kmbh,
    sum(b.jffse) jffse, sum(b.dffse) dffse
FROM cwhs_pz a, cwhs_jzpz b
WHERE a.jzpzh = b.jzpzh AND a.jzbj = 'jz'
  AND a.ztbh = #{ztbh} AND a.jzrq = #{jzrq} AND a.zzbj = #{zzbj}
GROUP BY a.jzlxfl, a.xtlx, a.ywlx,
         NVL(a.yhzhhm,'其他账号'), NVL(a.ywfl,'其他分类'),
         b.kmbh, SIGN(b.jffse), SIGN(b.dffse)
ORDER BY NVL(a.yhzhhm,'其他账号'), NVL(a.ywfl,'其他分类'), b.kmbh
```

## 5. 编译与提交

1. SDK 切换：`vfox use java@17 && vfox use maven@3.9.14`
2. 编译命令（在 `capinfo-gjj-busi-jshs` 仓根）：
   ```bash
   mvn -pl capinfo-gjj-busi-cwhs-jzgl -am clean compile
   ```
3. Git 提交说明末尾必须追加"（AI生成提交）"。建议拆为 4 个提交：
   - `feat(cwhs-jzgl): 凭证主表新增中心账号与业务分类字段（AI生成提交）`
   - `feat(cwhs-jzgl): ywjzPz 注入 yhzhhm/ywfl 并支持字典映射回退（AI生成提交）`
   - `feat(cwhs-jzgl): 凭证汇总查询按 yhzhhm+ywfl 聚合并保留 legacy 回退（AI生成提交）`
   - `docs: 凭证汇总改造 V1.1 实施操作记录与 SQL 脚本（AI生成提交）`

## 6. 未尽事项与遗留

1. `cwhs_pz` 另两条非 `ywjzPz` 的凭证生成入口（`CwhsServiceImpl.java` 8146 行、12329 行附近，使用 `ywcsConfig.getCwgljgbh()` 注入 `jbjgbh`）暂未注入 `yhzhhm/ywfl`，若涉及同样需要新维度的场景，后续可平移本次写法。
2. 监控指标（`ywfl_fallback_count` 等）需要在 metrics 层接入 Prometheus；当前仅落日志 `WARN`，方便手工筛查。
3. 数据治理：灰度上线后建议 DBA 抽样统计 `cwhs_pz.yhzhhm/ywfl` 的 NULL 率，向业务侧反馈推动字典 `codeZl1` 补齐。
