# 20260508 区域差异方法清单生成记录

## 1. 目标
- 在 `prod` 基线下，生成 `zaozhuang`、`linyi`、`wenzhou`、`jiaxing` 四个区域的“文件与方法差异清单”。

## 2. 执行结果
- 新增提示词文档：`doc/提示词/Gjj项目-区域差异方法清单提示词.md`
- 已在总提示词文档追加入口：`doc/提示词/Gjj项目组前后端项目开发提示词.md`
- 输出原始差异数据：`codeworkfiles/region_method_diff_report.json`

## 3. 差异统计
- `prod` 基线文件数：`703`
- `zaozhuang`：差异 `45`，新增文件 `20`，缺失文件 `160`
- `linyi`：差异 `59`，新增文件 `9`，缺失文件 `234`
- `wenzhou`：差异 `66`，新增文件 `26`，缺失文件 `241`
- `jiaxing`：差异 `87`，新增文件 `30`，缺失文件 `91`

## 4. 说明
- 本次为 Java 方法签名级别自动对比，用于快速定位区域改造点。
- 复杂场景（跨模块调用、SQL 语义变化）在合并前需做链路级复核。
