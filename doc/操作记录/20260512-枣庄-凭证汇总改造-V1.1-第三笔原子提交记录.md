# 20260512-枣庄-凭证汇总改造-V1.1-第三笔原子提交记录

## 1. 目标
- 对齐详细设计 V1.1 的任务 4“Mapper汇总改造”。
- 将自动汇总口径从按 `ywlx` 聚合切换为按 `yhzhhm + ywfl` 聚合。
- 保留旧版按 `ywlx` 聚合的回退能力，受配置 `voucher.summary.byYwfl` 控制。

## 2. 本次收口文件
- `capinfo-gjj-busi-cwhs-jzgl-basic-svc-api/src/main/java/cn/capinfo/gjj/busi/cwhs/jzgl/api/dto/req/PzhzReqDTO.java`
- `capinfo-gjj-busi-cwhs-jzgl-basic-svc-api/src/main/java/cn/capinfo/gjj/busi/cwhs/jzgl/api/dto/resp/JzpzhzRespDTO.java`
- `capinfo-gjj-busi-cwhs-jzgl-basic-svc-busi/src/main/java/cn/capinfo/gjj/busi/cwhs/jzgl/busi/domain/dao/mapper/JzpzDOMapper.java`
- `capinfo-gjj-busi-cwhs-jzgl-basic-svc-busi/src/main/java/cn/capinfo/gjj/busi/cwhs/jzgl/busi/domain/dao/mapper/JzpzDOMapper.xml`
- `capinfo-gjj-busi-cwhs-jzgl-basic-svc-busi/src/main/java/cn/capinfo/gjj/busi/cwhs/jzgl/busi/service/impl/CwhsServiceImpl.java`
- `capinfo-gjj-busi-cwhs-jzgl-basic-svc-busi/src/main/java/cn/capinfo/gjj/busi/cwhs/jzgl/busi/service/support/VoucherSummarySupport.java`
- `capinfo-gjj-busi-cwhs-jzgl-basic-svc-busi/src/test/java/cn/capinfo/gjj/busi/cwhs/jzgl/busi/service/support/VoucherSummarySupportTest.java`

## 3. 关键改动
- `JzpzDOMapper.xml#getJzpzhz` 改为按 `COALESCE(yhzhhm,'其他账号') + COALESCE(ywfl,'其他分类')` 聚合。
- 新增 `getJzpzhzLegacy`，用于配置回退到旧版按 `ywlx` 聚合逻辑。
- 新增 `getDjdjsHjByDim`，附件单据数按 `yhzhhm + ywfl` 维度统计。
- `CwhsServiceImpl#pzhz_fq` 增加 `voucher.summary.byYwfl` 开关切换。
- `CwhsServiceImpl#zdpzhz` 从按 `ywlx` 二级分组改为按 `yhzhhm + ywfl` 二级分组。
- 汇总摘要优先取业务分类名称；同一 `ywfl` 下多个 `ywlx` 名称不一致时，降级为 `业务分类:{ywfl}`。
- `pzhzJzbjXg` 更新条件由 `ywlx` 改为 `yhzhhm + ywfl`，并兼容空值归并到“其他账号/其他分类”。
- `VoucherSummarySupport` 补充维度兜底与摘要兜底纯函数，配套补充单元测试。

## 4. 验证结果
- 守卫扫描：
  - `scripts/guardrails/agent-delivery-guardrail.sh --scan-only`
  - 结果：通过，未发现偷懒式占位痕迹。
- 编译验证：
  - `export JAVA_HOME=/home/source/.vfox/cache/java/v-17.0.2+8/java-17.0.2+8`
  - `export MAVEN_HOME=/home/source/.vfox/cache/maven/v-3.9.14/maven-3.9.14`
  - `export PATH="$JAVA_HOME/bin:$MAVEN_HOME/bin:$PATH"`
  - `mvn -pl capinfo-gjj-busi-cwhs-jzgl/capinfo-gjj-busi-cwhs-jzgl-basic-svc-busi -am -DskipTests compile`
  - 结果：`BUILD SUCCESS`
- 单测验证：
  - `mvn -DskipTests=false -Dmaven.test.skip=false -Dtest=VoucherSummarySupportTest test`
  - 结果：未通过。
  - 原因：模块内存在既有 API DTO 缺失，编译阶段即报 `找不到符号`，报错集中在 `CwhsService`、`CwhsCwbbController`、`CwhsZbglController` 等文件，不是本次第三笔改动新增的问题。

## 5. 提交边界说明
- 本笔仅覆盖“汇总口径改造”闭环，不夹带 DDL、生成链路字段注入等已提交内容。
- `application.yml` 未纳入本笔提交；灰度开关已在区域配置存在，不重复引入无关配置改动。

## 6. 后续建议
- 提交并 push 本笔后，再进入任务 5/6 的测试、灰度或文档交付拆分。
- 若要补齐单测执行链路，需先修复当前模块缺失的 API DTO 依赖问题，再单独执行模块测试。
