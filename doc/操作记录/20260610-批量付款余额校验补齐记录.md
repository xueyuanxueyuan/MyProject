## 事项概述

- 初始需求：批量付款请求增加账户余额是否充足的校验，行为参考单笔付款。
- 本次返工需求：
  - 余额不足提示改为 `中心账户：{yhzhhm}，余额不足`
  - 中心账户判断移到付款限制参数之外
  - 未设置付款限制参数时也要校验中心账户
- 影响范围：`YwglServiceImpl` 中单笔付款余额提示与两个批量付款校验入口。

## 问题定位

- 单笔付款校验方法 `ckeckFkyw` 已包含“交易金额大于账户记账余额时转人工经办”的逻辑，但提示文案仍是旧文案。
- 批量付款校验方法 `checkFkywPlfk(FkywPlfkAddReqDTO)` 与 `checkFkywPlfk(Fkyw, List<FkywPlmx>)` 当前文件中虽然已有余额不足判断，但中心账户判断仍位于付款限制参数块内部。
- 用户反馈“没改成功”后复核确认：上一次修改未完整落到当前文件，实际代码与说明文档不一致。

## 修改说明

### 代码修改

- 在 `ckeckFkyw(Fkyw fkyw)` 中统一余额不足提示文案：
  - `中心账户：{yhzhhm}，余额不足`
- 在 `checkFkywPlfk(FkywPlfkAddReqDTO)` 中：
  - 增加 `validateBatchCounterpartyAccounts(...)` 前置校验
  - 将“收付方账号不能一样 / 对手账号不能是中心账号”移到付款限制参数校验前
  - 余额不足提示改为带中心账号的新文案
- 在 `checkFkywPlfk(Fkyw, List<FkywPlmx>)` 中同步做相同调整。
- 新增两个辅助方法：
  - `buildCenterAccountInsufficientMessage(String yhzhhm)`
  - `validateBatchCounterpartyAccounts(String payerAccount, List<String> payeeAccounts)`
- 余额不足时继续沿用既有处理方式：
  - `jblx` 置为人工经办
  - `spzt` 置为待审批
  - `jsxyxx` 置为带中心账号的新提示

### 对比留痕

- 对比文件：`doc/操作记录/20260610-批量付款余额校验补齐对比文件.md`

## 验证结果

### 已完成

- 代码复核确认三处余额不足提示都已改为新文案。
- 代码复核确认两处批量付款中心账户判断都已移到付款限制参数块之前。
- `GetDiagnostics` 检查目标文件，未发现由本次改动新增的语法错误。

### 未完成 / 受环境限制

- 待本次返工完成后重新执行 guardrail 与定向编译复核。

## 结论

- 本次已按用户最新要求修正真实代码，而不是停留在上一次不完整修改的说明上。
- 当前代码满足“提示文案更新”和“未设置付款限制参数也校验中心账户”的要求。
