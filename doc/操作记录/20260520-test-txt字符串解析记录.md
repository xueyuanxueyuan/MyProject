# test.txt 字符串解析记录

## 目标文件

- 文件路径：`/home/source/Jetbrains/Probject/Gjj/doc/操作记录/test.txt`
- 文件内容：单行超长字符串，总长度 `111830` 个字符。

## 解析结论

- 该字符串不是明文文本，而是一个 **十六进制编码** 的二进制数据。
- 将十六进制还原为字节流后，字节长度为 `55915`。
- 字节流以 `78 9C` 开头，符合 **zlib/deflate 压缩流** 常见特征。
- 使用 zlib 解压后，得到一个完整的 **PDF 1.5 文件**。
- PDF 由 `OpenPDF 1.3.32` 生成，创建时间为 `2026-05-20 17:38:24 +08:00`。
- PDF 共 `1` 页，页面大小为 `595 x 420 pts`，未加密。

## 解析过程

1. 读取原始字符串，确认全部由十六进制字符组成。
2. 使用 `bytes.fromhex(...)` 将字符串还原为原始二进制。
3. 使用 `zlib.decompress(...)` 成功解压。
4. 解压结果头部为 `%PDF-1.5`，尾部存在 `%%EOF`，可判定为结构完整的 PDF 文件。
5. 使用 `pdfinfo` 与 `pdftotext` 对生成的 PDF 做基础信息和文本提取验证。

## PDF 基础信息

```text
Producer:        OpenPDF 1.3.32
CreationDate:    Wed May 20 17:38:24 2026 CST
Pages:           1
Encrypted:       no
Page size:       595 x 420 pts (A5)
File size:       56975 bytes
PDF version:     1.5
```

## 提取出的主要文本

```text
中国农业银行住房公积金管理系统电子回单
账号：22401001046472133
户名：眉胜收月连鸳添幼慊关嗣
交易对手账号：963014013000060786
交易对手户名：山西公积金测试账号零零一
交易日期：20260516
交易金额：-1220000.00元
银行主机流水号：1522984024
电子回单编号：51115229840243586101
摘要：转取
附言：资金调拨
重要提示：该文件为中国农业银行电子回单，请勿重复打印或重复记账。
```

## 说明与边界

- 当前结论已通过实际解压和 PDF 工具提取验证，不是基于字符串猜测。
- `户名` 字段提取结果看起来像乱码或字体映射后的异常字符，可能与 PDF 内嵌字体编码方式有关；其余字段可正常识别。
- 本次仅完成“字符串载荷类型识别”和“可读文本提取”，未对 PDF 图像层、字体对象或签章信息做更深层解析。

## 二次复核补充

- 用户后续修改了 `test.txt`，该文件当前内容已经不是首次分析时的那一串。
- 新字符串长度为 `298870` 个字符，仍然是十六进制字符串，但其编码链路与第一次不同。
- 当前字符串的真实链路为：
  1. 十六进制字符串
  2. 还原为字节流
  3. zlib 解压
  4. 得到“PDF 字节数组的文本表示”，例如 `[37, 80, 68, 70, ...]`
  5. 将整数数组重新还原为字节后，得到真正的 PDF 文件
- 该 PDF 不是直接可提取正文的文本型 PDF，而是带表单/签章特征的图片式 PDF。
- 页面渲染后可直接确认其标题为 **山东农村商业银行 银行电子回单**，不是第一次分析得到的农行电子回单。

## 当前版本识别结果

- 文件类型：`PDF 1.4`
- PDF 大小：`119876` 字节
- 页数：`1`
- 是否加密：`否`
- 是否包含表单：`是（AcroForm）`
- 是否包含签名域：`是（字段名为 sig）`
- 生成器：`iText 1.4.4`，后续由 `iText 5.5.13` 修改

## 当前版本页面可见关键信息

- 标题：`山东农村商业银行 银行电子回单`
- 交易日期：`20260518`
- 交易时间：`10:24:40`
- 银行流水号：`420125216D00000000090456`
- 公积金中心账号：`2160014594205912345678`
- 公积金中心名称：`枣庄市住房公积金管理中心`
- 交易对手账户：`6223200406065130`
- 交易对手名称：`赵存冬`
- 交易对手银行：`枣庄农商银行`
- 金额（小写）：`-1.00`
- 手续费：`0.00`
- 交易说明：`公积金单位付款`
- 备注：`提取`

## 误差原因说明

- 第一次结论对应的是用户修改前的旧内容，当时解析链路和 PDF 内容都已验证，但不再适用于当前文件。
- 本次已重新读取最新 `test.txt` 原文，并基于当前内容重新解码、解压、还原 PDF 与页面渲染，不存在沿用旧缓存结果的情况。

## Base64 导出结果

- 已按当前文件内容成功还原最终 PDF 二进制，并转换为 Base64 字符串。
- Base64 完整结果文件：`/home/source/Jetbrains/Probject/Gjj/doc/操作记录/20260520-test-txt解码后base64.txt`
- 已额外直接导出可打开的 PDF 文件：`/home/source/Jetbrains/Probject/Gjj/doc/操作记录/20260520-test-txt解码后.pdf`
- 最终载荷字节长度：`119876`
- Base64 字符串长度：`159836`
- 最终载荷 `SHA-256`：`00355051832aebb4dc45550ab82862c92d0235213060b0e9c77af38fbc0e397d`
- Base64 前缀：`JVBERi0xLjQKJeLjz9MKMyAwIG9iago8PC9GVC9TaWcvVChzaWcpL1YgMSAwIFIvRiAxMzIvVHlwZS9Bbm5vdC9TdWJ0eXBlL1dpZGdldC9SZWN0WzAgMCAw`
- Base64 后缀：`YzYxY2U0NzUzNWMwMT48YzI4NDM0NmY0ZTg0MjI2NWRmOGE1MTIyOTQ5ZTMyZjc+XT4+CiVpVGV4dC01LjUuMTMKc3RhcnR4cmVmCjExOTQzMQolJUVPRgo=`

## Base64 转 PDF 排错结论

- 当前 Base64 文件经再次校验是**有效**的：
  - 长度：`159836`
  - `len % 4 = 0`
  - 仅包含合法 Base64 字符
  - 使用严格模式 `base64.b64decode(..., validate=True)` 可成功解码
- 解码后的文件头为 `%PDF-1.4`，并已通过 `pdfinfo` 验证，可正常识别为 PDF。
- 如果手工转换时报错，最常见原因不是内容损坏，而是**复制时拿到了截断内容**。
- 特别是从聊天预览、IDE 单行预览或终端输出里复制时，往往看到的是被截断的片段，而不是完整 `159836` 字符的 Base64。

## 正确转换方式

```bash
python3 - <<'PY'
from pathlib import Path
import base64

src = Path('/home/source/Jetbrains/Probject/Gjj/doc/操作记录/20260520-test-txt解码后base64.txt')
out = Path('/home/source/Jetbrains/Probject/Gjj/doc/操作记录/20260520-test-txt解码后.pdf')
out.write_bytes(base64.b64decode(src.read_text().strip(), validate=True))
print(out)
PY
```

## 三次复核补充

- 用户再次修改了 `test.txt`，当前内容与前两次相比新增了大量空格分段。
- 本次解析时先移除了所有空白字符，再进行后续识别。
- 当前文件的真实编码链路为：
  1. 含空格分段的十六进制字符串
  2. 去除空白后还原为字节流
  3. zlib 解压
  4. 得到一整段 Base64 字符串
  5. Base64 解码
  6. 得到最终 PDF 文件
- 与前一次不同的是：前一次 `test.txt` 本身保存的是“纯连续十六进制字符串”；这一次保存的是“带空格分段的十六进制字符串”。
- 但两次在去除空白并完成解码后，得到的最终 PDF **完全一致**。

## 当前版本最终结论

- 清洗空白后的十六进制长度：`210176`
- 还原后的压缩字节长度：`105088`
- zlib 解压后的 Base64 长度：`159836`
- 最终 PDF 字节长度：`119876`
- 最终 PDF `SHA-256`：`00355051832aebb4dc45550ab82862c92d0235213060b0e9c77af38fbc0e397d`
- 与 `/home/source/Jetbrains/Probject/Gjj/doc/操作记录/20260520-test-txt解码后.pdf` 逐字节比对结果：`完全一致`

## 四次复核补充

- 用户再次修改了 `test.txt`，当前内容已变为**无空格的纯连续十六进制字符串**。
- 本次无需做空白清洗，直接可按十六进制处理。
- 当前文件的真实编码链路为：
  1. 纯十六进制字符串
  2. 还原为字节流
  3. zlib 解压
  4. 得到一整段 Base64 字符串
  5. Base64 解码
  6. 得到最终 PDF 文件
- 本次解析得到的中间层与最终层摘要如下：
  - 十六进制长度：`210176`
  - 压缩字节长度：`105088`
  - zlib 解压后 Base64 长度：`159836`
  - 最终 PDF 长度：`119876`
  - 最终 PDF `SHA-256`：`00355051832aebb4dc45550ab82862c92d0235213060b0e9c77af38fbc0e397d`
- 与 `/home/source/Jetbrains/Probject/Gjj/doc/操作记录/20260520-test-txt解码后.pdf` 的逐字节比对结果仍为：`完全一致`
- 结论：当前版本与上一版的区别仅在**外层表示形式**，底层 PDF 载荷仍未变化。

## 五次复核补充

- 用户再次修改了 `test.txt`，当前内容已切回另一种外层包装。
- 当前文件的真实编码链路为：
  1. 纯十六进制字符串
  2. 还原为字节流
  3. zlib 解压
  4. 得到“整数数组文本”，形如 `[37, 80, 68, 70, ...]`
  5. 将整数数组映射回字节
  6. 得到最终 PDF 文件
- 本次解析得到的关键摘要如下：
  - 十六进制长度：`298870`
  - 压缩字节长度：`149435`
  - zlib 解压后整数数组文本长度：`546982`
  - 最终 PDF 长度：`119876`
  - 最终 PDF `SHA-256`：`00355051832aebb4dc45550ab82862c92d0235213060b0e9c77af38fbc0e397d`
- 该最终 PDF 经 `pdfinfo` 验证为：
  - `PDF 1.4`
  - `1` 页
  - `AcroForm`
  - 未加密
- 与 `/home/source/Jetbrains/Probject/Gjj/doc/操作记录/20260520-test-txt解码后.pdf` 逐字节比对结果：`完全一致`
- 结论：当前版本虽然中间层从 Base64 又切回了“整数数组文本”，但最终底层 PDF 仍未变化。

## 六次复核补充

- 用户再次修改了 `test.txt`，当前内容切换为另一份全新的载荷。
- 当前文件的真实编码链路为：
  1. 带空格分段的十六进制字符串
  2. 去除空白后还原为字节流
  3. zlib 解压
  4. 直接得到最终 PDF 文件
- 本次解析得到的关键摘要如下：
  - 原始字符长度：`92078`
  - 去空白后十六进制长度：`90662`
  - 压缩字节长度：`45331`
  - zlib 解压后 PDF 长度：`46580`
  - 最终 PDF `SHA-256`：`a5c27dbb8b36f06ac4a62c2eb9381cde1d9ca7465ecfdad103ce3c45e539015e`
- 该最终 PDF 经 `pdfinfo` 验证为：
  - `PDF 1.4`
  - `1` 页
  - 未加密
  - Producer：`iText 2.0.8 (by lowagie.com)`
- `pdftotext` 提取到的主要内容表明，这是一份**中国邮政储蓄银行业务凭证（付款凭证）**，主要字段包括：
  - 电子凭证号码：`403997108020002142812026052250390118000011`
  - 付款方户名：`嘉兴市公积金测试`
  - 收款方户名：`测擎测`
  - 付款方账号：`933044013000433376`
  - 收款方账号：`6217991000107199783`
  - 付款用途：`提取`
  - 摘要：`行内转出`
  - 交易流水号：`50390118`
  - 验证码：`AYA5278KX0323532`
  - 交易日期：`2026-5-22 14:45:09`
- 结论：当前版本不再复用前几版“农商行 PDF”载荷，而是另一份新的 **邮储银行付款凭证 PDF**。

## 七次复核补充

- 用户再次修改了 `test.txt`，当前内容已切换为另一份更大的新载荷。
- 当前文件的真实编码链路仍为：
  1. 纯十六进制字符串
  2. 还原为字节流
  3. zlib 解压
  4. 直接得到最终 PDF 文件
- 本次解析得到的关键摘要如下：
  - 原始字符长度：`466546`
  - 十六进制长度：`466546`
  - 压缩字节长度：`233273`
  - zlib 解压后 PDF 长度：`244085`
  - 最终 PDF `SHA-256`：`042eadc8f6e378ddf599998f28d785d4cb028422f46abe6db8bc02e2b5fde981`
- 该最终 PDF 经 `pdfinfo` 验证为：
  - `PDF 1.4`
  - `1` 页
  - `AcroForm`
  - 未加密
  - Producer：`iText® 5.5.6`，并带 `CFCA® 6.0.0` 修改痕迹
- `pdftotext` 提取到的主要内容表明，这是一份**浙商银行客户电子回单（贷）**，主要字段包括：
  - 付款人户名：`葛明`
  - 付款人账号：`6223093310082449425`
  - 收款人户名：`嘉兴公积金联调测试专户`
  - 收款人账号：`3310010010120183052118`
  - 金额：`人民币壹元整` / `￥1.00`
  - 业务种类：`中间业务接口-扣款`
  - 交易时间：`2026-05-22 14:46:48`
  - 受理编号：`86820260522149261361`
  - 主机流水号：`92170512`
  - 备注：`个人公积金贷款还款`
  - 防伪码：`426158216626584638126911`
- 结论：当前版本与上一版“邮储银行付款凭证 PDF”不同，已切换为新的 **浙商银行客户电子回单 PDF**。

## 可复现命令

```bash
python3 - <<'PY'
from pathlib import Path
import zlib

src = Path('/home/source/Jetbrains/Probject/Gjj/doc/操作记录/test.txt')
hex_text = src.read_text().strip()
pdf_bytes = zlib.decompress(bytes.fromhex(hex_text))
Path('/tmp/test_from_hex.pdf').write_bytes(pdf_bytes)
print('saved to /tmp/test_from_hex.pdf')
PY

pdfinfo /tmp/test_from_hex.pdf
pdftotext /tmp/test_from_hex.pdf -
```
