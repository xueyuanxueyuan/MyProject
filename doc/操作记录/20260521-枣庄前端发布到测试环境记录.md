# 20260521-枣庄前端发布到测试环境记录

## 1. 任务目标

- 将枣庄项目前端按当前本地工作区代码重新打包。
- 仅发布前端单包 `capinfo-gjj-frontend-jshs-gm.zip` 到枣庄测试环境 `zaozhuang:test`。
- 完成前端 deployment 重启与 rollout 校验。

## 2. 执行前确认

### 2.1 参考规范

- `doc/项目规范/打包编译发布规范.md`
- `doc/操作记录/20260420-枣庄测试环境前端发布记录.md`

### 2.2 当前前端工作区状态

- 工程目录：`zaozhuang/WebstormProjects/capinfo-gjj-frontend-jshs-gm`
- 当前分支：`zaozhuang/dev`
- 发布前检测到以下未提交内容：
  - `src/router/index.js`
  - `vue.config.js`
  - `package-lock.json`
- 已与用户确认：本次按当前本地前端工作区代码直接打包发布。

## 3. 打包过程

### 3.1 工具链切换

- `vfox use nodejs@16.20.2`
- `eval "$(vfox env -s bash)"`
- 校验结果：
  - `node -v` -> `v16.20.2`
  - `npm -v` -> `8.19.4`

### 3.2 前端构建

- 执行命令：
  - `rm -rf dist`
  - `npm install`
  - `npm run build`
  - `zip -rq capinfo-gjj-frontend-jshs-gm.zip dist`
- 构建结果：成功生成前端发布包。
- 构建过程中存在 4 条 warning，未阻塞本次打包：
  - `zskglglb.vue` 中引用 `getOrgList` 警告
  - `butterfly-dag` 的 CSS/SVG 解析警告
  - 资源体积过大 warning
  - 入口体积过大 warning

### 3.3 发布包归档

- 执行了 `zaozhuang/release` 目录中旧前端包清理。
- 已复制新的前端压缩包到：
  - `zaozhuang/release/capinfo-gjj-frontend-jshs-gm.zip`
- 归档后文件大小：
  - `6.1M`

## 4. 发布到枣庄测试环境

### 4.1 上传前端单包

- 命令：
  - `python3 scripts/remote_deploy.py zaozhuang:test capinfo-gjj-frontend-jshs-gm.zip`
- 结果：上传成功。

### 4.2 重启前端服务

- 目标 deployment：
  - `capinfo-gjj-frontend-jshs-gm-depl`
- 命令：
  - `python3 scripts/remote_exec.py zaozhuang:test "kubectl rollout restart deployment capinfo-gjj-frontend-jshs-gm-depl -n {namespace}"`
- 结果：
  - `deployment.apps/capinfo-gjj-frontend-jshs-gm-depl restarted`

### 4.3 Rollout 校验

- 命令：
  - `python3 scripts/remote_exec.py zaozhuang:test "kubectl rollout status deployment capinfo-gjj-frontend-jshs-gm-depl -n {namespace} --timeout=300s"`
- 结果：
  - `deployment "capinfo-gjj-frontend-jshs-gm-depl" successfully rolled out`

## 5. 执行结论

- 枣庄前端已按当前本地工作区代码完成重新打包。
- 前端发布包已成功上传到枣庄测试环境。
- 前端 deployment 已成功重启并通过 rollout 校验。

## 6. 后续建议

- 在枣庄测试环境打开本次改动涉及页面做功能回归。
- 重点关注路由入口与 `vue.config.js` 相关行为是否存在环境差异。
