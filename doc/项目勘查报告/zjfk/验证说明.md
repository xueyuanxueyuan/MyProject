# zjfk 分析交付验证说明

日期：2026-09-14。验证对象是本文档及项目图谱，不是业务系统上线验收。

## 1. 分析范围

- 源码根：`D:/Probject/Gjj/prod/IdeaProject/capinfo-gjj-busi-zjfk`。
- 提交号：`ea480f0687ff441a5e08422beb2095f28e3f5a4e`。
- 分析前及最终检查时，该独立业务仓 `git status --short` 均无输出。
- 根工作区原有技能、挡板程序等修改保留，本次产物限定在 `doc/项目勘查报告/zjfk/`，未提交或推送。

## 2. 已执行检查与结果

| 检查 | 实际结果 | 说明 |
|---|---|---|
| 生成器语法 | `node --check` 退出码 0 | 只验证新生成器语法 |
| 本地图谱生成 | 退出码 0；结构校验 PASS | 700 文件清点、487 文本索引；764 节点、1650 关系 |
| 关系完整性 | 所有边的两端均存在；证据文件和行号有效 | 不将结构合法等同于完整业务语义 |
| 统计交叉核对 | 24 控制器、218 Java 路由、13 Python 路由、38 有效建表、33 实体映射 | Python 挂载前缀单独核对；排除 SQL 第 411 行注释建表 |
| 源码引用检查 | 三份主体文档共 403 处完整路径/缩写路径引用有效 | 包括详细介绍、项目图谱、接口与数据索引；只检查路径和行号范围，不替代逐条语义审计 |
| 文本指纹 | 487 份 SHA-256 全部与当前源码一致 | 可发现生成后源码漂移 |
| XML 解析 | draw.io 与 SVG 均解析成功 | draw.io 为 29 个 mxCell：2 根单元、13 节点、14 连线 |
| 架构图渲染 | SVG 已通过本地图片库渲染并目视检查 | 中文标题、节点文字和连线可读；未启动 draw.io 应用 |
| Mermaid 图 | 8 个代码块已检查边界、节点与证据 | 未使用 Mermaid 渲染器逐图渲染，不宣称已完成可视化渲染测试 |
| 编码与占位扫描 | 产物无 UTF-8 BOM；文档未检出待补内容标记 | 文件以 UTF-8 无 BOM 保存 |
| 敏感模式扫描 | 未检出凭证赋值、JDBC 连接串或私有 IP 模式 | 仅扫描产物；模式扫描并非全面安全审计，未复制配置值 |
| Python 纯语法 | 58 文件 AST 解析完成，0 语法错误 | 使用桌面捆绑 Python，未导入业务模块、安装依赖或执行脚本 |
| 差异检查 | 指定文档目录 `git diff --check` 无输出、退出码 0 | 新增未跟踪文件另做编码、引用、占位和内容模式检查，不能仅凭该命令判定它们已审查 |

### Python 语法检查警告

`capinfo-gjj-busi-zjfk-model/train.py:285` 的 Windows 路径字符串触发多条无效转义 `SyntaxWarning`。解析没有失败，但这些警告确实存在；本次为分析任务，没有修改旧脚本。

最初默认终端找不到 `python`，随后定位桌面内置 Python 完成上述纯语法解析。这不代表业务 Python 依赖环境已经就绪。

## 3. 工具适配与校验修正

- 常见位置未找到可直接使用的 understand 插件已构建核心，本次采用本地结构抽取及人工分层图。产物明确使用自定义格式，不声称运行了 understand 全流程或完整 AST 调用分析。
- 仓库 `scripts/guardrails/agent-delivery-guardrail.sh` 不存在，因此执行了等价的范围、引用、占位、编码、敏感内容和图谱结构扫描，没有伪称原守卫脚本运行通过。
- 首轮 SQL 文本命中 39 次，复核发现一处在行注释中，已改为只统计有效行首建表，最终为 38。
- 生成器首次结构验证把空 Python 包文件误当成缺失证据，修正为空字符串可合法代表文件内容后，重新生成验证通过。
- Windows 补丁命令曾受长度/换行限制影响，改用标准 apply_patch 执行入口传入完整补丁；失败尝试未写入半成品。

## 4. 未做的验证

未执行 Java 编译和 Maven 测试；未安装 Python 依赖或执行训练/预测/HTTP 测试；未访问数据库、对象存储、Redis、工作流或调度中心；未启动容器或连接 Kubernetes；未验证前端、权限、财务口径、模型效果、性能、并发一致性和报告 DOCX 排版。

因此交付结论仅为：**项目分析文档、静态项目图谱与接口数据索引已生成，并通过列出的产物检查。业务功能运行状态和生产可用性未验证。**
