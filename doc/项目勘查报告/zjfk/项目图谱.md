# zjfk 项目图谱

基线：2026-09-14，提交 `ea480f0687ff441a5e08422beb2095f28e3f5a4e`。路径缩写见 `项目详细介绍.md`。本页是人工核对的概念与流程图；源码导航关系见 `knowledge-graph.json`。

## 1. 工程依赖图

箭头表示构建依赖或明确 HTTP 调用。Python 不属于 Maven 子模块。

```mermaid
flowchart LR
  APP["APP · Spring Boot 启动"] --> BUSI["BUSI · 业务实现"]
  BUSI --> API["API · DTO 与共享契约"]
  BUSI --> PLATFORM["平台基础组件与工作流依赖"]
  BUSI --> CLIENT["PythonModelClient"]
  CLIENT --> PYTHON["独立 FastAPI 模型服务"]
  PYTHON --> FACTORY["ModelFactory · GRU / LSTM / MLP / ARIMA / SVR"]
  PYTHON --> LEGACY["V1 ModelService · GRU 与流动性测算"]
```

证据：三个子模块 `pom.xml`、`B/mxgl/utils/PythonModelClient.java:68`、`P/app/main.py:48`、`P/app/services/model_service_v2.py:29`。

## 2. 业务能力全景

从左到右是能力分组，不代表每次请求均执行所有节点。实际业务链路可能由定时任务或人工操作分别触发。

```mermaid
flowchart TB
  PROJECT["住房公积金资金风控"]
  PROJECT --> CONFIG["配置管理 pzgl"]
  PROJECT --> MODEL["模型管理 mxgl"]
  PROJECT --> USE["模型预测与应用 mxyy"]
  PROJECT --> REPORT["资金报告 doc"]
  PROJECT --> OPS["调度与模型平台扩展"]
  CONFIG --> DATASOURCE["数据源维护 / 测试连接"]
  MODEL --> DATASET["指标 / 数据集 / 采集日志"]
  MODEL --> TRAIN["算法 / 模型 / 训练 / 比较"]
  USE --> FORECAST["指标预测 / 预警处置"]
  USE --> LIQUIDITY["流动性测算 / 历史版本"]
  USE --> PLAN["年度计划 / 月度计划 / 调整 / 审批"]
  USE --> DEPOSIT["定期存款规划 / 增值收益"]
  REPORT --> TEMPLATE["模板 / 参数 / 数据项 / 生成 / 下载"]
  OPS --> JOB["Java 任务与手工触发"]
  OPS --> KF["Kubeflow / KServe · 部分能力未完成"]
```

证据：24 个控制器的基础路径与方法映射，详见 `接口与数据索引.md` 控制器表。

## 3. 跨语言训练时序

这里展示 V2 训练主链与 V1 任务查询组合。返回任务号只代表受理，模型训练和业务记录回写在之后发生。

```mermaid
sequenceDiagram
  participant Caller as 调用方或任务
  participant Java as Java训练业务
  participant Client as PythonModelClient
  participant Route as FastAPI V2训练路由
  participant Task as TaskService
  participant Model as ModelServiceV2与算法
  participant Disk as 模型和JSON文件
  Caller->>Java: 发起训练
  Java->>Client: 模型、机构、指标、CSV与参数
  Client->>Route: POST /api/v2/models/train-v2
  Route->>Task: 创建TRAINING任务
  Task->>Disk: 保存初始任务
  Route-->>Client: 返回taskId
  Route->>Model: BackgroundTasks执行训练
  Model->>Disk: 保存模型、指标与图表结果
  Model->>Task: 更新完成或失败状态
  loop Java查询任务
    Java->>Client: 查询任务进度或结果
    Client->>Task: 经/api/v1/tasks路由查询
    Task->>Disk: 读取任务与结果
    Task-->>Java: 返回状态及结果
  end
  Java-->>Caller: 提供业务训练记录与结果
```

证据：`B/mxgl/utils/PythonModelClient.java:68`、`:121`、`:191`；`P/app/api/routes/models_v2.py:70`；`P/app/services/model_service_v2.py:145`。时序图为源码流程概括，不含事务一致性承诺。

## 4. 流动性测算与历史版本

```mermaid
flowchart LR
  INPUT["历史流动资金数据"] --> JAVA["Java测算业务"]
  JAVA --> PY["POST /api/v1/liquidity/optimize"]
  PY --> CLEAN["过滤非正数 / 样本检查"]
  CLEAN --> FIT["对数正态拟合 / KS检验"]
  FIT --> SIM["蒙特卡洛模拟"]
  SIM --> QUANTILE["10% / 50% / 90%分位数及图表"]
  QUANTILE --> RESULT["返回测算结果"]
  RESULT --> VERSION["Java保存历史会话和结果"]
  VERSION --> MANAGE["查询 / 激活 / 比较 / 清理"]
```

此图没有把“测算”画成收益最大化求解；版本管理由 Java 提供，不由 Python 训练任务承担。证据：`P/app/services/model_service.py:757`、`B/mxyy/ycjg/controller/ZjldzjcylcsController.java:45`。

## 5. 数据关系图（两种证据强度）

### 5.1 业务逻辑关联

本图箭头表示字段/业务关联，不表示数据库已创建外键，也不承诺所有关系的强制基数。

```mermaid
flowchart LR
  SOURCE["zjfk_data_source_config"] --> DATASET["zjfk_dataset"]
  INDICATOR["zjfk_indicator_type"] --> DATASET
  DATASET --> DATA["zjfk_dataset_data"]
  DATASET --> LOG["zjfk_dataset_collection_log"]
  ALGORITHM["zjfk_algorithm_type"] --> MODEL["zjfk_model"]
  DATASET --> MODEL
  MODEL --> TRAINING["zjfk_model_training"]
  MODEL --> COMPARE["zjfk_model_comparison_detail"]
  COMPARE --> GROUP["zjfk_model_comparison"]
  MODEL --> PREDICT["zjfk_prediction_result"]
  TYPE["zjfk_plan_type"] --> YEAR["zjfk_yearly_plan"]
  TYPE --> MONTH["zjfk_monthly_plan"]
  MONTH --> ADJUST["zjfk_monthly_plan_adjustment"]
  DEPOSIT["zjfk_fixed_deposit_plan"] --> DETAIL["zjfk_fixed_deposit_plan_detail"]
  TEMPLATE["zjfk_report_template"] --> PARAM["zjfk_report_template_parameter"]
  TEMPLATE --> ITEM["zjfk_report_template_data_item"]
  TEMPLATE --> REPORT["zjfk_generated_report"]
```

证据：`数据库设计/zjfk_db.sql` 相应建表字段及 33 个实体映射；逐表位置见 `接口与数据索引.md`。源脚本中没有直接实体映射的表也保留在索引中，不推断为废弃表。

### 5.2 DDL 明确声明的外键

```mermaid
flowchart LR
  HISTORY["zjfk_optimal_liquidity_history"] -->|"机构与版本组合外键"| SESSION["zjfk_liquidity_calculation_session"]
  VERSION["zjfk_kubeflow_model_version"] -->|"model_registry_id"| REGISTRY["zjfk_kubeflow_model_registry"]
  DEPLOY["zjfk_kubeflow_model_deployment"] -->|"model_registry_id"| REGISTRY
  DEPLOY -->|"model_version_id"| VERSION
```

证据：`数据库设计/zjfk_db.sql:579`、`:1051`、`:1102`。这是脚本关系，未连接实际数据库确认约束存在。

## 6. Kubeflow 能力边界图

实线表示有真实调用代码；虚线表示模拟、未完成或无法据此证明远端操作发生。所有真实调用也均未联调。

```mermaid
flowchart TB
  TASK["KubeflowTaskServiceImpl"] --> PIPE["KubeflowPipelineClient"]
  PIPE --> REMOTE["Pipeline HTTP API"]
  TASK -.->|"删除前终止"| SIM["KubeflowClient模拟终止"]
  TASK -.->|"暂停与恢复"| FALSE["回调返回false"]
  TASK --> LOG["KubeflowClient真实Pod日志"]
  COLLECT["ResourceMetricsCollectorJob"] --> METRIC["真实Pod指标采集"]
  KSERVE["KServeModelService"] --> REGISTRY["KubeflowModelRegistryClient"]
  REGISTRY --> CRD["Kubernetes InferenceService"]
  KSERVE -.->|"流量更新"| DBONLY["仅数据库比例更新"]
  KSERVE -.->|"异步监控"| GAP["未实现实际监控"]
```

证据：`B/kubeflow/service/impl/KubeflowTaskServiceImpl.java:183`、`:195`、`:215`；`B/kubeflow/service/KServeModelService.java:181`、`:365`；`B/kubeflow/client/KubeflowModelRegistryClient.java:389`。

## 7. 部署结构

```mermaid
flowchart LR
  SERVICE["Java Service 8080"] --> JVM["Java容器 9205"]
  CONFIG["Java ConfigMap"] --> JVM
  JVM --> DB["业务数据库"]
  JVM --> EXTDB["配置的数据采集来源"]
  JVM --> OBJECT["对象存储 / 文件能力"]
  JVM --> PYSERVICE["Python Service 8000"]
  PYSERVICE --> PY["Python容器 8000"]
  PYCONFIG["Python配置"] --> PY
  PVC["Python数据PVC"] --> PY
  JVM -.->|"按需开启"| KUBE["Kubeflow / Kubernetes"]
```

这是仓库配置和代码依赖视图，不是现网网络拓扑。未收录私有地址、账号或连接串。证据：`k8s/dev-tsp/` 两个部署文件、数据源服务与 `PythonModelClient`。

## 8. 机器图谱怎么使用

`knowledge-graph.json` 使用本项目自定义 `zjfk-static-graph/1.0` 格式，不伪装成 understand 插件原生输出，也没有进行完整 AST 分析。

| 关系 | 含义 | 不能据此推断 |
|---|---|---|
| contains | 项目、分组、文件包含关系 | 运行时加载或调用 |
| imports | Java 显式、可解析的内部导入 | 该方法一定执行或 Bean 一定注入 |
| maps_to | Java 实体映射数据库表 | 真实数据库表已经创建 |
| sql_references | Mapper XML 引用表名 | 所有动态 SQL 或精确读写范围 |
| defines_schema | SQL 非行注释建表声明 | 迁移已执行 |
| routes | Controller 或已挂载 Python 路由 | 鉴权通过、接口在线或测试通过 |
| depends_on | 当前 Maven 模块显式依赖 | 完整传递依赖树 |

每条关系包含源码文件与行号，inventory 带文本内容指纹。Python 算法内部调用没有自动抽取，已通过本页概念图补充；反射、动态 SQL、Feign 外部实现和框架隐式关系均不冒充完整覆盖。

按 endpoint 节点查接口，反查 `routes` 找 Controller；按文件节点的 `imports` 找静态类型依赖；按 table 节点查 `maps_to` 和 `sql_references` 找实体及 Mapper。搜索业务关键词则优先使用本文和详细介绍，不要在 764 个节点中盲目扩散。
