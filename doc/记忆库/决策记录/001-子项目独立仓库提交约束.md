# 子项目独立仓库提交约束

- **记录日期**：2026-08-27
- **结论**：`prod`、`jiaxing`、`linyi`、`wenzhou`、`zaozhuang` 的后端项目 `IdeaProject/capinfo-gjj-busi-jshs` 均拥有独立 Git 仓库。
- **提交规则**：涉及上述地区后端代码时，必须优先在对应地区的 `capinfo-gjj-busi-jshs` 子项目仓库中查看状态、暂存、提交和推送；不得将子项目源码作为根目录聚合仓库的未跟踪文件强制加入提交。
- **执行要求**：每个子项目独立核对分支、远端状态、暂存范围和提交结果；除非用户明确要求，不混合提交其他子项目或根目录文件。
- **来源**：用户于 2026-08-27 对五地区日记账摘要调整提交任务的明确说明；已通过本地 `.git` 元数据确认。
